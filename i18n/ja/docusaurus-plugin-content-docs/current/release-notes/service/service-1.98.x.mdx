---
id: service-1_98_x
title: Service 1.98.X
toc_max_heading_level: 3
---

import Status from '@site/src/components/Highlight';
import UI from '@site/src/components/UItext';

## Service-1.98.7

2023年4月20日

<div class='indentTab'>

### `共通`

-   <Status>Fixed</Status> アカウントアイコン選択 > <UI>アカウント情報管理</UI> > <UI>アカウント情報</UI> メニューで会員退会時の未払金額の表現及び経営支援チームの電話番号案内を修正

### `Application`

-   <Status>Fixed</Status> <UI>分析</UI> > <UI>マルチトランザクション追跡</UI>メニューで<UI>MTID / CUSTID</UI>値の未入力時にデータが検索されないバグを修正

-   <Status>Change</Status> <UI>分析</UI> > <UI>マルチトランザクション追跡</UI>メニューで<UI>MTID / CUSTID</UI>検索値の有効性チェックロジックを追加

</div>

## Service-1.98.6

2023年4月18日

<div class='indentTab'>

### `共通`

#### サイト管理

-   <Status>Change</Status> <UI>サイト管理</UI> > <UI>ビジネスボード</UI>メニューで<b>productType</b>を<code>platformType</code>に変更

-   <Status>Change</Status> <UI>サイト管理</UI> > <UI>決済管理</UI> > <UI>使用量</UI>メニューで<b>製品タイプ</b>カラムを除去

#### 利用履歴

-   <Status>Change</Status> <UI>利用履歴</UI> > <UI>請求・納付履歴</UI>メニューの<UI>請求内容の詳細を見る</UI>一覧で<b>製品タイプ</b>カラムを除去

</div>

2023年4月11日

<div class='indentTab'>

### `CloudWatch`、`NCP`、`OCI`、`Azure`

<p><Status>Fixed</Status> <UI>管理</UI> > <UI>エージェントインストール</UI>メニューで説明追加及びUI修正</p>

-   プロジェクト作成直後、エージェントインストールページに入ると表示される読み込み画像の下段に説明を追加

-   サービス一覧の照会・保存時に表示される読み込み画像の下段に説明を追加

-   `CloudWatch`インストール案内ページがスクロールに隠さずにすぐ表示されるように説明部分の高さを修正

</div>

## Service-1.98.5

2023年4月7日

<div class='indentTab'>

<p><Status>Fixed</Status> <UI>統計/レポート</UI> > <UI>レポート</UI>メニューで、ユーザーが選択した開始日基準ではなく、現在の基準だけでエージェント一覧を<UI>フィルター</UI>オプションとして取得するバグを修正</p>

</div>

## Service-1.98.4

2023年4月7日

<div class='indentTab'>

### `Application`

<p><Status>Fixed</Status> <UI>ヒートマップ</UI>メニューでトランザクションの詳細情報ウィンドウを表示すると<UI>マルチTXテーブル</UI>タブが表示されないバグを修正</p>

</div>

## Service-1.98.3

2023年4月6日

<div class='indentTab'>

### `Database`

<p><Status>Change</Status> <UI>分析</UI> > <UI>メトリクス異常検出</UI>メニューの非表示処理、<UI>サイトマップ</UI>メニューを通じてアクセス可能</p>

</div>

## Service-1.98.2

2023年4月5日

<div class='indentTab'>

### `共通`

<p><Status>Fixed</Status> <UI>アカウント情報管理</UI> > <UI>会員退会</UI>メニューを利用して会員退会する場合、退会の関連事項（未払金、権限情報など）をポップアップウィンドウで確認するよう修正</p>

### `Application`

<p><Status>Fixed</Status> <UI>ダッシュボード</UI> > <UI>アプリケーションダッシュボード</UI>メニューで言語設定が日本語の場合、要約ウィジェットのテキストが改行されずメトリクスが隠れるバグを修正</p>

</div>

## Service-1.98.1

2023年3月31日

<div class='indentTab'>

<p><Status>Fixed</Status> <UI>分析</UI> > <UI>メトリクスチャート</UI> メニューでオーバーレイチャートのCSVダウンロードのバグを修正</p>

</div>

## Service-1.98.0

2023年3月29日

<div class='indentTab'>

### `共通`

#### UX/UI

ユーザーの満足度を高めるために、次のようにユーザーインターフェースデザインを改善します。

-   <Status>Change</Status> <UI>利用履歴</UI> > <UI>請求書プレビュー</UI> メニューのUI改善

    -   プロジェクト、製品別にグループ化された請求書プレビュー情報の提供
    -   グループ化されたデータの関連商品情報及び課金の集計量、請求額の合計などの確認が可能

-   <Status>Change</Status> <UI>利用履歴</UI> > <UI>プロジェクト有料プラン転換</UI> メニューのUI改善

-   <Status>Change</Status> 画面上段に位置する「メニューパス」を「メニュー名」として表示するように変更

    -   変更前：上位パス > メニュー名
    -   変更後：メニュー名

-   <Status>Change</Status> 画面右上のWhaTap技術文書のリンクボタン追加およびチャンネルトーク内の技術文書リンクを除去

-   <Status>Fixed</Status> サービスロゴの横のブラウザー時間の不一致通知アイコンの位置を修正

-   <Status>Fixed</Status> プロジェクトのないグループを選択すると、プロジェクト作成ページに移動しないように修正

-   <Status>Deprecate</Status> <UI>統合ダッシュボード</UI>、<UI>統合メトリックボード</UI> メニューは2023年5月1日付で左サイドメニューから除去、<UI>サイトマップ</UI>へ移転予定に関する案内を表示

#### Flexボード

-   <Status>Change</Status> ダッシュボードと一覧を画面に読み込む時に、読み込みインジケーターを適用

-   <Status>Feature</Status> Seriesウィジェットタイプのチャート設定で凡例表示を追加

-   <Status>Feature</Status> Seriesウィジェットタイプに累積チャートタイプを追加

-   <Status>Feature</Status> Seriesウィジェットタイプのチャート設定でチャート色基準のオプションを追加

    -   従来：タグを基準にチャートの色を表現
    -   追加：フィールドを基準にチャートの色を表示するオプション

-   <Status>Feature</Status> DB製品群で次のウィジェットテンプレートを追加

    -   **DB共通**
        -   CPU %（xos）ウィジェット
        -   Memory %（xos）ウィジェット

    -   **MySQL**
        -   スロークエリ

    -   **PostgreSQL**
        -   スロークエリ
        -   PostgreSQL Wait Event件数ウィジェット
        -   PostgreSQL Client Ip件数ウィジェット
        -   PostgreSQL統計テーブル
        -   PostgreSQLアクティブセッション件数
        -   PostgreSQLアクティブセッションテーブル

#### MXQL

<p><Status>Fixed</Status> MXQLクエリ内のフィールド名に特殊文字が入る場合、MXQLデータ呼び出し時に発生するバグの修正のため、MXQLクエリ生成ロジックを修正</p>

#### ログ

-   <Status>Change</Status> <UI>ログ</UI> メニューの機能を容易に理解するため、メニュー名を変更

    -   <b>ライブTail</b> → <UI>ライブテール</UI>
    -   <b>探索機</b> → <UI>ログトレンド</UI>
    -   <b>検索機</b> → <UI>ログ検索</UI>

-   <Status>Change</Status> <UI>ログ検索</UI> メニューで照会内容を <UI>Timestamp</UI> を基準にソートする機能（<UI>新しい順</UI>/<UI>古い順</UI>）に変更

-   <Status>Change</Status> <UI>ログトレンド</UI> メニューで検索キーを最大2つまで設定できるように修正（検索値は制限なし）

-   <Status>Change</Status> <UI>ログトレンド</UI> メニューで照会時間を変更した時に、選択したフィルターに対する結果がない場合、上記のフィルターを解除できるよう改善

#### アラート通知

-   <Status>Feature</Status> <UI>イベント設定</UI> メニューで <UI>エージェント重複通知</UI> オプションを追加

    > モニタリング中のエージェントのうち、同じIDを設定したエージェントが2つ以上ある場合、メトリクスを重複して収集することによって発生する警告通知です。 エージェント名が重複して設定されていないかエージェント設定で関連オプションを確認してください。ブルー/グリーンのようなデプロイ状況でエージェントが重複して警告通知が発生した場合、この機能を利用することをお勧めします。 同名のエージェントが同時に2つ以上起動した場合、選択した時間中は通知が発生しません。

-   <Status>Feature</Status> <UI>イベント受信設定</UI> メニューで <UI>受信タグの未設定通知を受け取る</UI> オプションを追加

    > イベント受信タグが設定されていない警告通知を受信しない場合は、そのオプションを選択してください。

-   <Status>Fixed</Status> <UI>イベント受信設定</UI> メニューで登録したモバイル機器の数が表示されないバグを修正

-   <Status>Fixed</Status> <UI>イベント設定</UI> メニューでログイベントを追加する時の <UI>イベント発生一時中止</UI> 項目に対する説明文の修正

#### 管理

-   <Status>Feature</Status> ユーザー権限で <b>ログ照会</b> 権限の追加

-   <Status>Feature</Status> <UI>プロジェクト管理</UI> メニューで警告通知の言語選択オプションを追加

-   <Status>Fixed</Status> メンバー招待ウィンドウで <b>メンバー管理</b> 権限をオフできないバグを修正

-   <Status>Fixed</Status> チャンネルトーク > WhaTap支援チームの招待機能が動作しないバグを修正

    :::note

    WhaTap支援チームの招待ボタンは、<b>メンバー管理</b>権限を保有しているユーザーに限って表示されます。

    :::

-   <Status>Fixed</Status> <UI>メンテナンス計画</UI> メニューで <UI>毎月</UI> タブをクリックすると発生するホワイトアウト現象を修正

-   <Status>Change</Status> <UI>アカウント情報管理</UI> > <UI>会員退会</UI> メニューを利用して会員退会する場合の注意事項の最新化及び退会関連事項（未納金、権限情報）などの確認手続きを追加

#### 利用履歴

<UI>プロジェクト有料プラン転換</UI> メニューで次の機能を追加・修正します。

-   <Status>Feature</Status> プロジェクトを複数選択して有料プランに転換する機能を追加

-   <Status>Change</Status> <UI>月間予想費用</UI> カラムで製品別の詳細を確認できるように修正

#### 製品カタログ

-   <Status>Feature</Status> プロジェクト追加時 <UI>プロジェクトグループ</UI> 項目にグループ検索機能を追加

-   <Status>Change</Status> 組織を選択した状態でプロジェクトを追加する場合 <UI>プロジェクトグループ</UI> 項目を必須選択するように変更

### `Application`

#### アプリケーションダッシュボード

-   <Status>Fixed</Status> ヒートマップウィジェットでデータ呼び出しが中断される現象を修正

-   <Status>Fixed</Status> アクティブトランザクションウィジェットですべてのエージェントを非活性化する場合、チャートを更新しない問題を修正

#### 統計/レポート

<p><Status>Change</Status> <UI>統計</UI> メニューで照会時間の範囲を最大62日から <b>最大31日</b>に変更</p>

### `Server`

#### ダッシュボード

<p><Status>Fixed</Status> <UI>リソースボード</UI> メニューで <b>Top N イコライザー</b> チャート画面サイズが大きい場合、Y軸が長く表示される現象を修正</p>

#### サーバー一覧

<p><Status>Feature</Status> <UI>サーバー詳細</UI> メニューで <UI>プロセス</UI> テーブルのソートオプションとして <UI>個数の降順</UI> を追加</p>

#### サーバー設定

<p><Status>Change</Status> <b>リモートファイルビューアー</b> メニュー名称を <UI>エージェントログビューアー</UI>として変更</p>

#### ログ

<p><Status>Fixed</Status> <UI>ログ設定</UI> メニューのエージェントの設定オプションを修正</p>

#### アラート通知

-   <Status>Fixed</Status> 通知をコピーするとき、 <UI>通知ポリシーコピー</UI> ウィンドウで選択できるプロジェクト一覧の一部が欠落しているバグの修正

-   <Status>Feature</Status> <UI>イベント記録メニュー</UI>から <UI>最後の変更時刻表示</UI> オプション追加

-   <Status>Feature</Status> <UI>警告通知</UI> > <UI>イベント受信設定</UI> メニューの <UI>3rd パーティープラグイン</UI> 設定に <UI>エスカレーション</UI>オプションを追加

    -   Escalationを<UI>エスカレーション</UI>に翻訳
    -   オプションを修正すると有効性チェックを進めるように修正
    -   オプション値を変更して保存しない場合、ボタンの色を変更するように修正

### `Container`

-   <Status>Change</Status> <UI>管理</UI> > <UI>エージェントインストール</UI> ページUIおよび内容改善

-   <Status>Fixed</Status> <UI>ダッシュボード</UI> > <UI>コンテナマップ</UI>の<UI>フィルター</UI>オプションで<UI>含む</UI>を選択すると、部分一致するコンテナが表示されない現象を修正

-   <Status>Fixed</Status> <UI>ダッシュボード</UI> > <UI>コンテナマップ</UI> メニューで <img src='/img/ico-reset.svg' alt='아이콘' /> リセットボタンをクリックするとオプションが初期化されない現象を修正

### `Database`

#### データベース共通

-   <Status>Feature</Status> <UI>統計/レポート</UI> > <UI>SQL統計</UI> メニューに次のテーブル機能を追加

    -   テーブルのカラムサイズの調整を可能に
    -   ページを離脱しても調整したカラムのサイズが解除されないように設定を保存
    -   テーブルヘッダーの各カラム項目を選択すると、選択基準に従って一覧をソート

-   <Status>Fixed</Status> <UI>管理</UI> > <UI>エージェントインストール</UI> メニューの案内メッセージの位置を修正

-   <Status>Fixed</Status> <UI>警告通知</UI> > <UI>ファイルログ通知</UI> メニューでログが発生していない時間帯を照会すると、以前のログデータが残っている現象を修正

#### PostgreSQL、MySQL

<p><Status>Feature</Status> <UI>統計/レポート</UI> > <UI>データベースサイズ</UI> 新規UI適用</p>

#### Tibero

<p><Status>Change</Status> <UI>統計/レポート</UI> > <UI>テーブルスペースサイズ</UI> メニューでテーブルスペースの使用量に応じたプログレスバーの色を変更</p>

### `AWS`

<p><Status>Fixed</Status> <UI>イベント記録</UI> メニューを選択すると、関連のないスナップショットページに移動するバグ修正</p>

### `AWS` <sup>（CloudWatch）</sup>、`Azure`、 ```NCP``、 ```OCI\`

-   <Status>Change</Status> プロジェクト内のサービスごとに収集周期を設定できるように機能とUIを修正

-   <Status>Feature</Status> 活性化されたサービスのみを表示するフィルターを追加

-   <Status>Feature</Status> サービス名の検索ウインドウを追加

<!-- 

아래 내용은 Beta 상태, 설치형 내용으로 docs 릴리스 노트 배포에 포함하지 않습니다.

### `DATABASE` - 설치형

#### POSTGRESQL

- `Fixed` ***PG 대시보드 > 인스턴스 모니터링***  Active Session, Transactions 위젯의 단위 삭제 <허진권>
- `Fixed` *PG **대시보드 > 인스턴스 모니터링***  액티브 세션 테이블의 필터 목록 변경 <허진권>
- `Fixed` *PG **대시보드 > 인스턴스 모니터링***  query_text 컬럼의 query 클릭 시 팝업되는 SQL 보기 모달창에 파라미터가 표시되도록 수정 <허진권>
- `Feature` ***PG 대시보드 > 인스턴스 모니터링, PG 대시보드 > 멀티 인스턴스 모니터링*** 하단 액티브 세션 테이블에 세션 킬 기능 추가 <허진권>
- `Feature` ***PG 대시보드 > 멀티 인스턴스 모니터링*** 신규페이지 적용 <원성철>
- `Feature` PG ***대시보드 → 인스턴스 목록*** 하단 테이블에 프로세스 정보 탭 추가 <허진권>
- `Fixed` PG 인스턴스 목록 페이지에서 체크박스 체크 후 멀티뷰 버튼 클릭 시 멀티 인스턴스 모니터링 신규페이지로 이동 <허진권>
- `Change` ***대시보드 > 인스턴스*** ***모니터링*** 신규 UI 화면으로 변경<원성철>
- `Change` ***분석 > 락트리*** 신규 UI 화면으로 변경<원성철>
- `Feature` ***통계/보고서 > PG_STAT_STATEMENT*** 메뉴 추가 <허진권, 설치형에 옵션 적용시에만 적용>
    - Postgresql의 pg_stat_statements 뷰 정보 조회(DB에서 수행된 쿼리들에 대한 실행 통계 정보)

#### Mysql RDS - beta

- `Feature` ***대시보드 > 인스턴스 모니터링***  <원성철>
    - 락트리  ******하단 ******테이블 추가
        - DB 락일 발생한 경우 Tree Table 로 보여줍니다.
    - 프리셋 저장시 Active Session 선택한 컬럼 순서까지 저장하도록 수정
- `Feature` ***분석 > 락트리***  메뉴 추가<원성철>
    - DB 락일 발생한 경우 Tree Table 로 보여줍니다.
- `Fixed` 제품 안내 문구 수정 및 ***관리 > 에이전트 설치*** 페이지 방화벽 오픈 IP 수정 <허진권>
- `Change` ***대시보드 > 멀티 인스턴스*** ***모니터링*** 메뉴의 UI 텍스트 수정 <허진권>
- `Change` ***대시보드 > 인스턴스 목록*** 메뉴 및 ***대시보드 > 멀티 인스턴스 모니터링 메뉴의*** 기능 개선 <허진권>
    - ***대시보드 > 인스턴스 목록***  메뉴에서 ***멀티뷰*** 버튼을 클릭하면 ***신규 멀티 인스턴스 모니터링*** 페이지로 이동하도록 수정
    - ***대시보드 > 인스턴스 목록*** 메뉴와 ***대시보드 > 멀티 인스턴스 모니터링*** 메뉴 사이에 선택한 인스턴스 설정을 연동하도록 수정
    - ***대시보드 > 멀티 인스턴스 모니터링*** 메뉴에서 멀티 인스턴스 선택 및 변경사항을 저장하도록 수정

### `RUM`

#### Flex 보드

- `Fixed` 대시보드의 URL 그룹 필터가 정상 적용되지 않는 문제 수정 및 필터 위치 이동
- `Fixed` 환경별 대시보드의 브라우저별, OS별, 디바이스별 바(bar) 차트 위젯 MXQL 버그 수정
- `Fixed` 일부 위젯 중 필드 기준으로 차트 컬러가 설정되지 않는 위젯 수정

#### 분석

- `Feature` ***분석*** > ***에러 추적*** 메뉴 추가
    
    > 웹 애플리케이션 브라우저 레벨에서 발생하는 에러를 수집하여 확인할 수 있습니다.
    > 
- `Fixed` 스택 정보가 정상 표시되지 않는 문제 수정

-->

</div>
