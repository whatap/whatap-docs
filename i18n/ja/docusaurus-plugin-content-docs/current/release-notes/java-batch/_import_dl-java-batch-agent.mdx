:::note

Java Batchエージェントを最初にインストールする場合は、次のコマンドを使用してパッケージを最初にインストールします。 エージェントインストールの詳細については、[次の文書](../../java/add-jvm-opt/batch-app)を参照してください。

```bash
wget https://api.whatap.io/agent/whatap.agent.batch.tar.gz
```

:::
