---
id: agent-update
title: エージェントのアップデート
description: Kubernetesエージェントをアップデートする方法について案内します。
tags:
  - Kubernetes
  - Kubernetesモニタリング
  - エージェントのアップデート
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

エージェントのアップデートが必要な場合は、次のコマンドを実行してください。

<Tabs groupId='k8stype'>

<TabItem value='kubernetes' label='KUBERNETES' default>

-   **マスターエージェント**

    ```bash
    kubectl set image -n whatap-monitoring deployment/whatap-master-agent whatap-master-agent=whatap/kube_mon:{agent_version}
    ```

-   **ノードエージェント**

    ```bash
    kubectl set image -n whatap-monitoring daemonset/whatap-node-agent whatap-node-agent=whatap/kube_mon:{agent_version}
    ```

</TabItem>

<TabItem value='eksfragate' label='EKS Fargate'>

**マスターエージェント**

```
kubectl set image deployment/whatap-master-agent whatap-master-agent=whatap/kube_mon:{agent_version}
```

</TabItem>

</Tabs>

:::note

WhaTap Kubernetesエージェントの最新バージョンは、[hub.docker.com](https://hub.docker.com/r/whatap/kube_mon/tags)で確認できます。

:::
