- 基本情報: Podのクラスター名、ネームスペース、起動時間、現在の状態(Phase) などを確認できます。

- Kubernetesイベント: 最近のPodに関連するイベントログで、問題が発生したかを確認できます。

- 実行主体: Podを生成したコントローラー(DaemonSet, Deploymentなど)を識別します。

- ノード情報: Podが実行中のノードのOS、カーネルバージョン、CPU、メモリなどの詳細な仕様を提供します。

- リソース使用状況: Pod内のすべてのコンテナのCPUとメモリ使用量、設定されたリソース限度(Limit)と比較した使用率を示しています。
