- 基本情報: Podのクラスター名、ネームスペース、起動時間、現在の状況(Phase) などを確認できます。

- Kubernetesイベント: 最近のPodに関連するイベントログから問題の発生有無を確認できます。

- 実行情報: Podを生成したコントローラー(DaemonSet、Deploymentなど)を識別します。

- ノード情報: Podが実行中のノードのOS、カーネルバージョン、CPU、メモリなどの詳細な仕様を提供します。

- リソース使用状況: Pod内のすべてのコンテナのCPUとメモリ使用量、設定されたリソース限度(Limit)と比較した使用率を示しています。
