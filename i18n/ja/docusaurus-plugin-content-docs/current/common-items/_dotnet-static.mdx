-   **stat_domain_enabled** <span class='type'>Boolean</span><br/>デフォルト値 `false`<br/>
    クライアントに接続中のドメイン別トランザクション統計収集機能を活性化します。

-   **stat_domain_max_count** <span class='type'>Int</span><br/>デフォルト値`7000`<br/>
    5分間収集するドメイン別トランザクション統計の最大レコード数です。

<!-- 

* **stat_mtrace_enabled** <span class='type'>Boolean</span><br/>기본값 `false`<br/>
멀티 서버 트랜잭션에서 Caller & Callee 간에 버전별 의존성 통계를 수집합니다.

* **stat_mtrace_max_count** <span class='type'>Int</span><br/>기본값 `7000`<br/>
멀티 서버 트랜잭션에서 Caller & Callee 간에 버전별 의존성 통계의 최대 레코드 수입니다.

-->

-   **stat_referer_enabled** <span class='type'>Boolean</span><br/>デフォルト値`false`<br/>
    Referer別のトランザクション統計を収集します。

-   **stat_referer_max_count** <span class='type'>Int</span><br/>デフォルト値`7000`<br/>
    5分間収集するReferer別トランザクション統計の最大レコード数です。
