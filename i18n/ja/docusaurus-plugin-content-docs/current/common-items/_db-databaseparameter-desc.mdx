ホーム画面 &gt; プロジェクト選択 &gt; <Cmdname sid="side_analysis" className="uitext" /> &gt; <Cmdname sid="side_dbParameter" className="uitext" />

データベース設定値の参照機能を提供し、過去の日付の時点でのデータベース設定を表示することができます。比較機能を使用すると、データベースのパラメータ値を日付別に比較できます。デフォルトでは、現在時刻のデータが表示されます。 

データベースにパラメータ値が原因で問題が発生した場合、データベースパラメータ比較機能を使用すると、すばやく原因を特定できます。

<ProdImg img='db-databaseparameter.png' desc='データベースパラメータ'/>

1. <Cmdname sid="reference_date" className="uitext" />と<Cmdname sid="comparison_date" className="uitext" />を設定します。

2. <Cmdname sid="instance" className="uitext" />項目で目的のインスタンスを選択します。

3. ![検索アイコン](/img/ico-btn-search.png)ボタンをクリックします。 

現在と選択した過去の日付との変更点を一目で確認する場合は、<Cmdname sid="view_changes" className="uitext" />ボタンを選択します。

<ProdImg img='db-databaseparameter-changed.png' desc='データベースパラメータ'/>

<Xclude product="redis,mongodb,oracle-pro,oracle,db2,sapase">

:::tip

**今日の日付のデータベースパラメータを比較する**

<Xclude product="cubrid"><ProdImg img="db-databaseparameter-compare-today.png" desc="パラメータ" /></Xclude>

<Cmdname sid="reference_date" className="uitext" />と<Cmdname sid="comparison_date" className="uitext" />をすべて今日の日付に設定すると、リアルタイムデータと保存された最後のデータを照会して比較できます。データの最終保存時刻は、データベースサーバーの環境によって異なる場合があります。

:::

</Xclude>
