LinuxコンテナのCPU、MEMORY、DISK、NIC性能を収集します。収集された性能情報は、<Cmdname sid="category" className="uitext" />`server_container`で確認できます。

```bash title='Linux Shell'
echo "container.enabled=true" |sudo tee -a /usr/whatap/infra/conf/whatap.conf
```
