## ログの確認

-   _dbx.log_

    エージェントの起動および収集に関するログはすべて_`$WHATAP_HOME`/logs/dbx.log_パスで確認できます。_dbx.log_ファイルのサイズが25MBを超えると、_dbx.log.[n]_ファイル名でバックアップします。ファイルの整理が必要な場合は、数字の大きい（古い）ファイルから削除してください。

-   _xos.log_

    XOSの起動に関するログは、_`$XOSインストールパス`/xos`ymmd`.log_ファイルに記録します。ログは日付別に作成します。
