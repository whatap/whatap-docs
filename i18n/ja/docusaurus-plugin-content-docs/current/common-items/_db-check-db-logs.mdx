## ログの確認

-   _dbx.log_

    エージェントの起動および収集に関するログはすべて、_`$WHATAP_HOME`/logs/dbx.log_パスで確認できます。_dbx.log_ファイルのサイズが25MBを超える場合は、_dbx.log.[n]_ファイル名でバックアップします。ファイルをクリーンアップする必要がある場合は、サイズの大きい(古い) ファイルから削除してください。

-   _xos.log_

    XOSの起動に関するログは、_`$XOSインストールパス`/xos`ymmd`.log_ファイルに記録します。ログは日付で作成します。
