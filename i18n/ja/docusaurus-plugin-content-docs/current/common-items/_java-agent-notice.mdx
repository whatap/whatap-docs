-   **recursive_event_interval** <span class='type'>MiliSeconds</span><br/>
    デフォルト値`300000`<br/>
    トランザクションの再帰呼び出しに対し、イベント通知の発行間隔を設定します。

-   **reject_event_enabled**<span class='type'>Boolean</span><br/>
    デフォルト値 `false`<br/>
    サービス拒否（呼び出し負荷制限／拒否）の時、イベント通知を発行する設定をします。

-   **reject_event_interval**<span class='type'>MiliSeconds</span><br/>
    デフォルト値`300000`<br/>
    サービス拒否（呼び出し負荷制限／拒否）の時、イベント通知を発行する間隔を設定します。

-   **httpc_event_enabled** <span class='type'>Boolean</span><br/>
    デフォルト値`false`<br/>
    HTTPCの接続エラーが発生した場合、イベント通知を発行するかどうかを設定します。

-   **httpc_event_interval** <span class='type'>MiliSeconds</span><br/>
    デフォルト値`300000`<br/>
    HTTPCの接続エラーが発生した場合、イベント通知を発行する間隔を設定します。

-   **heap_event_enabled** <span class='type'>Boolean</span><br/>
    デフォルト値`false`<br/>
    ヒープ使用量がしきい値に達したとき、イベント通知を発行するかどうかを設定します。

-   **heap_event_percent**<span class='type'>Percentage</span><br/>
    デフォルト値`90`<br/>
    ヒープ使用量イベント通知のしきい値を設定します。

-   **heap_event_duration** <span class='type'>MiliSeconds</span><br/>
    デフォルト値`30000`<br/>
    ヒープ使用量イベント通知の発行持続時間を設定します。

-   **heap_event_interval** <span class='type'>MiliSeconds</span><br/>
    デフォルト値`300000`<br/>
    ヒープ使用量イベント通知の発行間隔を設定します。

-   **heap_event_action** <span class='type'>String</span><br/>
        デフォルト値`NONE`<br/>
        ヒープ使用量イベント発生したとき、実行する動的ローディングコードを設定します。
    :::note
    _`$WHATAP_HOME`/plugin/ActionScript.x_ ファイルに作成したJavaコードに伝えるID (`$id`で伝送)
    :::

-   **disk_event_enabled**<span class='type'>Boolean</span><br/>
    デフォルト`false`<br/>
    ディスク使用量がしきい値に達したとき、イベント通知を発行するかどうかを設定します。

-   **disk_event_percent**<span class='type'>Percentage</span><br/>
    デフォルト値`90`<br/>
    ディスク使用量イベント通知の基準しきい値を設定します。

-   **disk_event_interval** <span class='type'>MiliSeconds</span><br/>
    デフォルト値`300000`<br/>
    ディスク使用量イベント通知の発行間隔を設定します。

-   **disk_event_action** <span class='type'>String</span><br/>
        ディスク使用量イベントが発生したとき、実行する動的ローディングコードを設定します。
    :::note
    _`$WHATAP_HOME`/plugin/ActionScript.x_ ファイルに作成したJavaコードに伝えるID (`$id`で伝送)
    :::

-   **cpu_event_enabled** <span class='type'>Boolean</span><br/>
    デフォルト値`false`<br/>
    CPU使用量がしきい値に達したとき、イベント通知を発行するかどうかを設定します。

-   **cpu_event_percent**<span class='type'>Percentage</span><br/>
    デフォルト値'90\`<br/>
    CPU使用量イベント通知の基準しきい値を設定します。

-   **cpu_event_duration**<span class='type'>MiliSeconds</span><br/>
    既定値`30000`<br/>
    CPU使用量イベント通知の発行基準の持続時間を設定します。

-   **cpu_event_interval**<span class='type'>MiliSeconds</span><br/>
    デフォルト値`300000`<br/>
    CPU使用量イベント通知の発行間隔を設定します。

-   **cpu_event_action**<span class='type'>String</span><br/>
    CPU使用量イベント発生した時、実行する動的ローディングコードに転送するIDを設定します。
    :::note
    _`$WHATAP_HOME`/plugin/ActionScript.x_ファイルに作成したJavaコードに転送するID(\`$id'で転送)
    :::

-   **dbc_dup_event_enabled**<span class='type'>Boolean</span><br/>
    デフォルト`false`<br/>
    DB Connectionが重複割り当てられる場合、イベント通知を発行するかどうかを設定します。

-   **dbc_dup_event_fullstack_enabled** <span class='type'>Boolean</span><br/>
    デフォルト値`false`<br/>
    DB Connectionが重複割り当てられるときに、Stackを確保するかどうかを設定します。

-   **exception_event_enabled** <span class='type'>Boolean</span><br/>
    デフォルト値`false`<br/>
    Exceptionが発生した場合、イベント通知を発行するかどうかを設定します。

-   **exception_event_interval** <span class='type'>MiliSeconds</span><br/>
    デフォルト値`60000`<br/>
    Exceptionが発生した場合、イベント通知の発行間隔を設定します。

-   **exception_event_set** <span class='type'>String</span><br/>
    対象Exceptionを設定します。 複数の場合は、コンマ（,）を区切り文字として使用してください。

-   **exception_event_action**<span class='type'>String</span><br/>
        イベント発生時に実行する動的ローディングコードを設定します。
    :::note
    _`$WHATAP_HOME`/plugin/ActionScript.x_ファイルに作成したJavaコードに送信するID（`$id`で送信）
    :::
