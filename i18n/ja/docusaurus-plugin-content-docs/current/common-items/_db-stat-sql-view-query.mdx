## クエリ別の実行推移を確認

<Xclude product='postgresql,mysql,oracle,cubrid,mssql,mssql-v1,oracle-pro,oracle-v1,altibase'>

テーブルの項目別の左の►ボタンを選択します。クエリ文の5分当たりの実行回数と平均実行時間が表示されます。各棒グラフにカーソルを合わせると、その時間の数値が表示されます。

利用区分ごとにデータを分類して表示するには、目的のタブを選択してください。

</Xclude>

<InDoc product='postgresql,mssql,cubrid,oracle-v1,altibase,mssql-v1'>

使用カテゴリ別にデータを分類して表示するには![数字2](/img/number-02.png)で目的のタブを選択します。

テーブルの項目別の左の![数字3](/img/number-03.png)►ボタンを選択します。クエリ文の5分当たりの実行回数と平均実行時間が表示されます。各棒グラフにカーソルを合わせると、その時間の数値が表示されます。

</InDoc>

<InDoc product='mysql,oracle,oracle-pro'>

使用区分ごとに希望するデータを分類して照会したり、ソート順を変更することができます。希望するタブをクリックしてください。

テーブルの項目別の左の►ボタンを選択します。クエリ文の5分当たりの実行回数と平均実行時間が表示されます。各棒グラフにカーソルを合わせると、その時間の数値が表示されます。

</InDoc>

<InDoc product='mysql'>

| ソート基準            | 説明                |
| ---------------- | ----------------- |
| elapse avg (sec) | 平均実行時間が最も長い値からソート |
| execute count    | 実行回数の多い順にソート      |
| elapse sum (sec) | 総実行時間が長い値からソート    |
| elapse max (sec) | 最大実行時間が長い値からソート   |

</InDoc>

<InDoc product='oracle,oracle-pro'>

| ソート基準                 | 説明                         |
| --------------------- | -------------------------- |
| elapse avg (sec)      | 平均実行時間が最も長い値からソート          |
| execute count         | 実行回数の多い順にソート               |
| elapse sum (sec)      | 総実行時間が長い値からソート             |
| elapse max (sec)      | 最大実行時間が長い値からソート            |
| elapse wait           | クエリの待ち時間が長い順にソート           |
| physical read         | ディスクから読み取ったデータブロックが多い順にソート |
| session logical reads | メモリから読み取ったデータブロックが多い順にソート  |

</InDoc>

<InDoc product="postgresql,mysql,altibase,db2,sapase">

:::note

グラフチャートで目的の時間の棒グラフを選択すると、<Cmdname sid="side_dbMonitoring" className="uitext" />メニューに移動します。選択した時間の指標を確認できます。<Cmdname sid="side_dbMonitoring" className="uitext" />の詳細については、[次の文書](instance-monitoring)を参照してください。

:::

</InDoc>

<InDoc product="oracle,cubrid">

:::note

グラフチャートで目的の時間の棒グラフを選択すると、<Cmdname sid="side_dbTrend" className="uitext" />メニューに移動します。選択した時間の指標を確認できます。<Cmdname sid="side_dbTrend" className="uitext" />の詳細については、[次の文書](analysis-count-trend)を参照してください。

:::

</InDoc>

## クエリプレビュー

![クエリ文プレビュー](/img/db-sql-preview.png)

照会された結果リストからマウスポインタを**_sql_**カラム項目に移動すると、クエリ文をプレビューできます。
