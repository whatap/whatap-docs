CPU、MEMORY、DISK、NICの仕様とインストールされたパッケージおよび仮想マシンのメタ情報を収集します。
各**_カテゴリ_**で、次の内容を確認することができます。

- `server_package`：インストールされているパッケージです。
- `server_inventory`：CPU、MEMORY、DISK、NICの仕様です。
- `server_pending_security_update`：Linuxのセキュリティパッチの待機リストです。

<Tabs>
<TabItem value="lxs" label="Linux Shell">

```bash title=SH
echo "inventory.product.enabled=true" |sudo tee -a /usr/whatap/infra/conf/whatap.conf
```

</TabItem>
<TabItem value="wps" label="Windows Powershell">

```bash title=SH
# 管理者権限が必要です。
Add-Content -Path "C:\Program Files\WhatapInfra\whatap.conf" -Value "inventory.product.enabled=true"
```

</TabItem>
</Tabs>
