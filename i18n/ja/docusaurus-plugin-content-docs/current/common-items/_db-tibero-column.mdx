import ProcessColumns from "../common-items/_db-process-column.mdx";

<Title level={props.level} hashid="column-info" >カラム情報ガイド</Title>

<InDoc pages="instance-monitoring,multi-instance-monitoring,analysis-count-trend">

カラム情報に関する詳しい内容は、[次のリンク](https://technet.tmax.co.kr/upload/download/online/tibero/pver-20220224-000002/index.html)を参照してください。

<Tabs groupId="db-columns">

<TabItem value="activesession" label="Active session">

| 項目                        | 説明                          |
| ------------------------- | --------------------------- |
| `アクション`                   | 実行中のタスク名です。                 |
| `block_updates`           | ブロックのアップデート回数です。            |
| `block_updates_sigma`     | ブロックアップデートの合計回数です。          |
| `client_identifier`       | クライアント識別子です。                |
| `client_info`             | クライアントの追加情報です。              |
| `client_pid`              | クライアントプロセスのPIDです。           |
| `command`                 | セッションで実行されているコマンドです。        |
| `consumed_cpu_time`       | セッションで使ったCPUの時間です。          |
| `consumer_group`          | セッションが属する消費者グループです。         |
| `event_name`              | 待機イベントの名前です。                |
| `execute_count`           | SQLが実行された回数です。              |
| `execute_count_sigma`     | 実行回数の合計です。                  |
| `ipaddr`                  | クライアントのIPアドレスです。            |
| `logon_time`              | セッションがデータベースにログインした時間です。    |
| `logical_reads`           | 論理的な読み取りの回数です。              |
| `logical_reads_sigma`     | 論理的な読み取りの合計回数です。            |
| `machine`                 | セッションが実行されているマシン名です。        |
| `module`                  | 実行中のモジュールの名前です。             |
| `os_thr_id`               | OSのスレッドIDです。                |
| `osuser`                  | セッションを実行しているOSのユーザーです。      |
| `parse_count_hard`        | ハードパーシングの回数です。              |
| `parse_count_hard_sigma`  | ハードパーシングの合計回数です。            |
| `parse_count_total`       | パーシングの合計回数です。               |
| `parse_count_total_sigma` | パーシングの合計回数です。               |
| `pdml_enabled`            | 並列DMLのアクティブ有無です。            |
| `pdml_status`             | 並列DMLの状態です。                 |
| `pddl_status`             | 並列DDLの状態です。                 |
| `pga_used_mem`            | プロセス専用領域(PGA)で使用中のメモリ量です。   |
| `physical_reads`          | 物理的な読み取り回数です。               |
| `physical_reads_sigma`    | 物理的な読み取りの合計回数です。            |
| `pq_status`               | 並列クエリの状態です。                 |
| `prev_sql`                | 以前に実行されたSQL文のテキストです。        |
| `prev_sql_id`             | 以前に実行されたSQL文の固有IDです。        |
| `prev_sql_param`          | 以前に実行されたSQLのパラメータです。        |
| `prog_name`               | セッションを実行するプログラムの名前です。       |
| `row_wait_block_no`       | 待機中の行のブロック番号です。             |
| `row_wait_file_no`        | 待機中の行のファイル番号です。             |
| `row_wait_obj_id`         | 待機中の行のオブジェクトIDです。           |
| `row_wait_row_no`         | 待機中の行の行番号です。                |
| `schemaname`              | セッションが使用されているスキーマの名前です。     |
| `seq#`                    | セッションの順番を示す値です。             |
| `sql_et`                  | SQL実行時間です。                  |
| `sql_hash`                | SQL文のハッシュ値です。               |
| `sql_id`                  | 実行中のSQL文の固有IDです。            |
| `sql_param`               | SQL実行に使用されたパラメータです。         |
| `sql_text`                | 実行中のSQL文のテキストです。            |
| `sql_trace`               | SQLトレーシング有無です。              |
| `state`                   | セッションの状況です。                 |
| `status`                  | セッションの状態です。                 |
| `terminal`                | セッションが接続されているターミナルの名前です。    |
| `type`                    | セッションのタイプです。                |
| `usn`                     | アンドゥ(undo) セグメント番号です。       |
| `used_blk`                | 使用されたブロック数です。               |
| `used_rec`                | 使用されたレコード数です。               |
| `wait_event`              | セッションが待機中のイベントです。           |
| `wait_time`               | 待機中の時間です。                   |
| `wlock_wait`              | セッションがロックを待機しているかどうかを表示します。 |
| `wthr_id`                 | セッションのタスクスレッドIDです。          |

</TabItem>

<TabItem value="locktree" label="Lock tree">

| 項目             | 説明                                                              |
| -------------- | --------------------------------------------------------------- |
| `Instance`     | インスタンス（エージェント）名                                                 |
| `sid`          | セッションID                                                         |
| `serial#`      | セッションの一意な識別子と共に使用するセッションシリアル番号                                  |
| `holder_type`  | ロックを保持しているセッションのロックタイプ（例：TX、TMなど）                               |
| `holder_mode`  | 保持中のロックモード（例：0～6、6はexclusive）                                   |
| `waiter_type`  | ロックを待機しているセッションのロックタイプ                                          |
| `waiter_mode`  | 現在要求中のロックモード                                                    |
| `id1`          | ロック識別子1（ロック対象オブジェクトを識別するために使用、例：トランザクションIDなど）                   |
| `id2`          | ロックの詳細情報またはバージョン情報を表します                                         |

</TabItem>

<TabItem value="process-info" label="Process information">

<ProcessColumns />

</TabItem>

</Tabs>

</InDoc>

<InDoc pages="analysis-lock-and-deadlock">

各セッションの詳細については、次のカラムを通じて提供します。

| 項目               | 説明                          |
| ---------------- | --------------------------- |
| `event`          | 待機イベントの名前です。                |
| `holder type`    | ロックを所有しているセッションのタイプです。      |
| `id1`            | 1番目の識別子の値です。                |
| `id2`            | 2番目の識別子の値です。                |
| `lock mode`      | セッションでリクエストまたは所有するロックモードです。 |
| `lock request`   | セッションでリクエストされたロックの状態です。     |
| `module`         | 実行中のモジュールの名前です。             |
| `pid`            | データベースプロセスのPIDです。           |
| `prev_sql_id`    | 以前に実行されたSQL文の固有IDです。        |
| `prev_sql_param` | 以前に実行されたSQLのパラメータです。        |
| `prev_sql_text`  | 以前に実行されたSQL文のテキストです。        |
| `prog_name`      | セッションを実行するプログラムの名前です。       |
| `serial#`        | 同じセッションのリトライを区分する一連番号です。    |
| `sid`            | セッションを固有に識別するセッションIDです。     |
| `sql_et`         | SQL実行時間です。                  |
| `sql_id`         | 実行中のSQL文の固有IDです。            |
| `sql_param`      | SQL実行に使用されたパラメータです。         |
| `sql_text`       | 実行中のSQL文のテキストです。            |
| `status`         | セッションの状態です。                 |
| `username`       | セッションを実行するユーザーの名前です。        |
| `waiter type`    | ロックを待っているセッションのタイプです。       |

</InDoc>

<InDoc pages="analysis-pq-tree">

| 項目               | 説明                       |
| ---------------- | ------------------------ |
| `block_updates`  | ブロックのアップデート回数です。         |
| `client_pid`     | クライアントプロセスのPIDです。        |
| `event`          | 待機イベントの名前です。             |
| `logon_time`     | セッションがデータベースにログインした時間です。 |
| `logical_reads`  | 論理的な読み取りの回数です。           |
| `machine`        | セッションが実行されているマシン名です。     |
| `module`         | 実行中のモジュールの名前です。          |
| `physical_reads` | 物理的な読み取り回数です。            |
| `pid`            | データベースプロセスのPIDです。        |
| `prev_sql_param` | 以前に実行されたSQLのパラメータです。     |
| `prev_sql_text`  | 以前に実行されたSQL文のテキストです。     |
| `prog_name`      | セッションを実行するプログラムの名前です。    |
| `seq#`           | セッションの順番を示す値です。          |
| `sid`            | セッションを固有に識別するセッションIDです。  |
| `sql_et`         | SQL実行時間です。               |
| `sql_param`      | SQL実行に使用されたパラメータです。      |
| `sql_text`       | 実行中のSQL文のテキストです。         |
| `status`         | セッションの状態です。              |
| `usn`            | アンドゥ(undo) セグメント番号です。    |
| `used_blk`       | 使用されたブロック数です。            |
| `used_rec`       | 使用されたレコード数です。            |
| `username`       | セッションを実行するユーザーの名前です。     |
| `wait_time`      | 待機中の時間です。                |

</InDoc>

:::note

WhaTapは、クライアントに関する情報をデフォルトで保存します。

:::
