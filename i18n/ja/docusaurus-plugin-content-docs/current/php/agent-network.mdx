---
id: agent-network
title: エージェントの通信設定
description: アプリケーションサーバーの通信を設定する方法を提供します。
tags:
  - PHP
  - 通信
  - 設定
  - アプリケーション
---

PHPアプリケーションサーバーの通信を設定する方法を提供します。

-   **whatap.server.host** <span class='type'>ip_address</span>

    デフォルト値`127.0.0.1,127.0.1`

    エージェントが収集したデータを送信するサーバーを指定します。収集サーバーを冗長化して2つ以上のIPがある場合、コンマ(,)で区分して指定できます。指定されたIPは、収集サーバーのproxyデーモンがリスニング状態でサービスする必要があります。

-   **whatap.server.port** <span class='type'>tcp_port</span>

    デフォルト値`6600`

    収集サーバーのポート番号を指定します。指定できるポートは1つしかないため、`whatap_server_host`に割り当てられた収集サーバーは同じポート番号を使用する必要があります。

-   **whatap.tcp_so_timeout** <span class='type'>Millisecond</span>

    デフォルト値`60000`

    収集サーバーと通信するTCPセッションのSocket Timeout値を指定します。

-   **whatap.tcp_connection_timeout** <span class='type'>Millisecond</span>

    デフォルト値`5000`

    収集サーバーと通信するTCPセッションのConnection Timeout値を指定します。

-   **whatap.net_send_max_bytes** <span class='type'>Byte</span>

    デフォルト値`5242880`

    収集サーバーにデータを送信する場合、一度に送信される最大サイズを指定します。

-   **whatap.net_udp_port** <span class='type'>tcp_port</span>

    デフォルト値`6600`

    WhaTapエージェントは、トレーサーからUDP経由で収集したデータを収集サーバーに送信します。最初のUDPサーバーのポート番号を指定できます。デフォルト値として提供される 6600番ポートが使用されている場合は、このオプションを使用します。PHPコンパイルのインストールで、_php.ini_にwhatap項目がある場合、_whatap.ini_の代わりに_php.ini_に追加してください。

{@include: ../common-items/_php-restart-apply.mdx} 
