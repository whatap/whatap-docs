---
id: agent-remove
title: エージェント削除
description: サーバーエージェントの削除方法について説明します。
keywords:
  - Server
  - 削除
  - 管理
---

### Linux

インストールされたエージェントを削除するには、次のコマンドを実行してください。

- **Debian / Ubuntu**
  ```bash title=SH
  sudo apt-get remove whatap-infra
  ```
- **CentOS / Amazon Linux / SUSE**
  ```bash title=SH
  sudo yum remove whatap-infra
  ```

### Unix

- **FreeBSD**

  ```bash title=SH
  pkg delete whatap-infra
  ```

- **AIX**

  ```bash
  stopsrc -s whatap-infra
  rpm -e whatap-infra
  rm -rf /usr/whatap/infra
  ```

- **Oracle Solaris**

  ```bash
  pkgrm whatap-infra
  rm -rf /usr/whatap/infra
  ```

- **HP-UX**

  ```bash title='IA64 / PA-RISC'
  swremove whatap-infra
  ```

### Windows

- サーバーモニタリングエージェントの削除コマンドを管理者権限のcommandウィンドウで実行します。 
  ```bash title=SH
  "C:\Program Files\WhatapInfra\unins000.exe"
  ```
- コントロールパネルのプログラム削除機能を使用することができます。
