---
id: agent-remove
title: エージェント削除
description: サーバーエージェントの削除方法について案内します。
tags:
  - Server
  - 削除
  - 管理
---

### Linux

インストールされたエージェントを削除するには、次のコマンドを実行してください。

-   **Debian / Ubuntu**
    ```bash title=SH
    sudo apt-get remove whatap-infra
    ```
-   **CentOS / Amazon Linux / SUSE / XenServer**
    ```bash title=SH
    sudo yum remove whatap-infra
    ```
-   **FreeBSD**
    ```bash title=SH
    pkg delete whatap-infra
    ```

### Windows

-   サーバーモニタリングエージェントの削除コマンドを管理者権限のcommandウィンドウで実行します。 
    ```bash title=SH
    "C:\Program Files\WhatapInfra\unins000.exe"
    ```
-   コントロールパネルのプログラム削除機能を使用することもできます。
