---
id: service-1_98_x
title: Service 1.98.X
toc_max_heading_level: 3
---

import Status from '@site/src/components/Highlight';
import UI from '@site/src/components/UItext';

## Service-1.98.7

April 20, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> In Select Account Icon > <UI>Manage Account INFO.</UI> > <UI>My Profile</UI>, modified to display the unpaid amount and management support team's phone number during membership withdrawal.

### `Application`

-   <Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Multiple Transaction Trace</UI>, fixed the bug where not entering the <UI>MTID / CUSTID</UI> value does not search data.

-   <Status>Change</Status> In <UI>Analysis</UI> > <UI>Multiple Transaction Trace</UI>, added the validation logic for the <UI>MTID / CUSTID</UI> search value.

</div>

## Service-1.98.6

April 18, 2023

<div class='indentTab'>

### `Common`

#### Site Management

-   <Status>Change</Status> In <UI>Site Management</UI> > <UI>Business Board</UI>, changed the term from <b>productType</b> to <code>platformType</code>.

-   <Status>Change</Status> In <UI>Site Management</UI> > <UI>Billing Management</UI> > <UI>Usage</UI>, deleted the <b>Product Type</b> column.

#### My Usage

-   <Status>Change</Status> In <UI>My Usage</UI> > <UI>Billing and payment history</UI>, deleted the <b>product type</b> column from the list in <UI>Display Billing History</UI>.

</div>

April 11, 2023

<div class='indentTab'>

### `CloudWatch`, `NCP`, `OCI`, `Azure`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, added the descriptions and modified UIs.</p>

-   Added a description at the bottom of the loading image displayed when entering the agent installation page immediately after a project creation.

-   Added a description at the bottom of the loading image displayed when searching and saving the service list.

-   Modified the height of the description so that the `CloudWatch` installation guide page appears immediately without being covered by the scroll.

</div>

## Service-1.98.5

April 7, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In <UI>STAT/REPORT</UI> > <UI>REPORT</UI>, fixed the bug where the <UI>Filter</UI> option fetches the list of agents only based on the current time rather than the start date selected by the user.</p>

</div>

## Service-1.98.4

April 7, 2023

<div class='indentTab'>

### `Application`

<p><Status>Fixed</Status> In <UI>HITMAP</UI>, fixed the bug where the <UI>Multi TX Table</UI> tab does not appear when the transaction details window appears.</p>

</div>

## Service-1.98.3

April 6, 2023

<div class='indentTab'>

### `Database`

<p><Status>Change</Status> Made <UI>Analysis</UI> > <UI>Metric Anomaly Detection</UI> invisible; the menu can be accessed using <UI>Sitemap</UI>.</p>

</div>

## Service-1.98.2

April 5, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> Through <UI>Manage Account INFO.</UI> <UI>Withdrawal</UI>, modified to check the membership withdrawal information (unpaid amount, granted roles).</p>

### `Application`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Application Dashboard</UI>, fixed the bug where the metric is hidden because the text in the summary widget does not wrap lines when the language is set to Japanese.</p>

</div>

## Service-1.98.1

March 31, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Metrics Chart</UI>, fixed the CSV download bug for the overlay chart.</p>

</div>

## Service-1.98.0

March 29, 2023

<div class='indentTab'>

### `Common`

#### UX/UI

To increase the user satisfaction, we improved the user interface design as follows.

-   <Status>Change</Status> In <UI>My Usage</UI> > <UI>Preview Invoice</UI>, improved the UIs.

    -   Provision of invoice previews grouped by project and product
    -   It is possible to check the related products of the grouped data, total billing amount, and total invoice amount.

-   <Status>Change</Status> In <UI>My Usage</UI> > <UI>Project Subscription</UI>, improved the UIs.

-   <Status>Change</Status> Changed the `menu path` at the top of the screen to `menu name`.

    -   Before change: Top Path > Menu Name
    -   After change: Menu Name

-   <Status>Change</Status> Added a WhaTap technical document link on the upper right of the screen and removed the technical document link in Channel Talk.

-   <Status>Fixed</Status> Repositioned the browser time discrepancy notification icon next to the service logo.

-   <Status>Fixed</Status> Fixed not to go to the project generation page when a group with no project is selected.

-   <Status>Deprecate</Status> The <UI>Integrated Dashboard</UI> and <UI>Integrated Metrics Board</UI> menus have been removed from the left side menu after May 1, 2023, and the notice to move it to <UI>Sitemap</UI> has been displayed.

#### Flex Board

-   <Status>Change</Status> Applied the loading indicator when loading the dashboard and list on the screen.

-   <Status>Feature</Status> Added a legend display in the settings of the chart of Series widget type.

-   <Status>Feature</Status> Added a stacked chart type in the series widget types.

-   <Status>Feature</Status> Added the `chart color by` option in the settings of the chart of Series widget type.

    -   Previous: Display of chart colors based on the tag
    -   Addition: Option to color charts by field

-   <Status>Feature</Status> Added the following widget templates in DB suite products.

    -   **DB commons**
        -   CPU % (xos) widget
        -   Memory % (xos) widget

    -   **MySQL**
        -   Slow Query

    -   **PostgreSQL**
        -   Slow Query
        -   PostgreSQL Wait Event Cout widget
        -   PostgreSQL Client Ip Count widget
        -   PostgreSQL Statistics table
        -   PostgreSQL Active Session Count
        -   PostgreSQL Active Session table

#### MXQL

<p><Status>Fixed</Status> Modified the MXQL query generation logic to fix the bugs that occur when calling the MXQL data while special characters are used in the field names for MXQL queries.</p>

#### Log

-   <Status>Change</Status> Changed the menu name for quick understanding of the <UI>Log</UI> menu's feature.

    -   <b>라이브 Tail</b> → <UI>Live Tail</UI>
    -   <b>Explorer</b> → <UI>Log Trend</UI>
    -   <b>Search</b> → <UI>Log Search</UI>

-   <Status>Change</Status> Changed the function to sort the searched contents by <UI>Timestamp</UI> in <UI>Log Search</UI> (<UI>Recent order</UI>/<UI>Past order</UI>).

-   <Status>Change</Status> Modified to set up to 2 search keys in <UI>Log Trend</UI> (the search value is not limited).

-   <Status>Change</Status> In <UI>Log Trend</UI>, improved to release the previously selected filters if there is no result for the filter when the search time has been changed.

#### Alert

-   <Status>Feature</Status> In <UI>Event Setting</UI>, added the <UI>Agent duplicate events</UI> option.

    > This alert occurs while collecting metrics redundantly when there are multiple agents with the same ID set among the agents being monitored. Check the related options in the agent settings to make sure there are no duplicate agent names. It is recommended to use this function when an alert occurs due to duplicate agents in deployment such as blue and green. If multiple agents with the same name are launched at the same time, you are not notified for the selected time.

-   <Status>Feature</Status> In <UI>Notification Setting</UI>, added the <UI>Receive notifications with no receive tag set</UI> option.

    > Not to receive alerts for which the Event Receiving Tag has not been set, select the option.

-   <Status>Fixed</Status> In <UI>Notification Setting</UI>, fixed the bug where the number of registered mobile devices is not displayed.

-   <Status>Fixed</Status> In <UI>Event Setting</UI>, added the description of the <UI>Event pause</UI> item upon addition of log events.

#### Management

-   <Status>Feature</Status> Added the <b>Log Read</b> role in user roles.

-   <Status>Feature</Status> In <UI>Project Management</UI>, added the alert language selection option.

-   <Status>Fixed</Status> Fixed the bug where the <b>Member Management</b> role cannot be turned off in the member invitation window.

-   <Status>Fixed</Status> In Channel Talk > Invite WhaTap Support, fixed the bug where the invitation function does not work.

    :::note

    The WhaTap Support invitation button is exposed to only the users with the <b>Member Management</b> role.

    :::

-   <Status>Fixed</Status> In <UI>Maintenance Plan</UI>, fixed the problem where a whiteout occurs when the <UI>Monthly</UI> tab is clicked.

-   <Status>Change</Status> Through <UI>Manage Account INFO.</UI> > <UI>Withdrawal</UI>, added the confirmation steps upon membership withdrawal such as updating notices and the related information (unpaid amount, granted roles).

#### My Usage

In <UI>Project Subscription</UI>, added and modified the following functions.

-   <Status>Feature</Status> Added the function to select multiple projects and convert them to subscription.

-   <Status>Change</Status> In the <UI>Monthly Estimation</UI> column, modified to see the detailed information for each product.

#### has been changed to product catalog

-   <Status>Feature</Status> Added the group search function in <UI>Project Group</UI> for addition of a project.

-   <Status>Change</Status> Modified to select a <UI>project group</UI> when adding a project with an organization selected.

### `Application`

#### Application Dashboard

-   <Status>Fixed</Status> Fixed the problem where data call is stopped in the hitmap widget.

-   <Status>Fixed</Status> Fixed the problem where disabling all agents in the active transaction widget does not update any chart.

#### STAT/REPORT

<p><Status>Change</Status> In <UI>Statistics</UI>, changed the lookup range from the maximum of 62 days to <b>the maximum of 31 days</b>.</p>

### `Server`

#### Dashboard

<p><Status>Fixed</Status> In <UI>Resource Board</UI>, fixed the problem where the Y-axis is displayed long when the screen size of the <b>Top N equalizer</b> chart is large.</p>

#### Server List

<p><Status>Feature</Status> In <UI>Server Detail</UI>, added <UI>Count Descending</UI> as an aligning option in the <UI>process</UI> table.</p>

#### Server Setting

<p><Status>Change</Status> The <b>Remote File Viewer</b> menu name has been changed to <UI>Agent Log Viewer</UI>.</p>

#### Log

<p><Status>Fixed</Status> In <UI>Log Setting</UI>, modified the agent configuration option.</p>

#### Alert

-   <Status>Fixed</Status> Fixed the bug where in case of copying an alert, some projects that can be selected in <UI>Copy Policy</UI> are missing.

-   <Status>Feature</Status> In <UI>Event History</UI>, added the option to <UI>show the last change time</UI>.

-   <Status>Feature</Status> In <UI>Alert</UI> > <UI>Notification Setting</UI>, added the <UI>escalation</UI> option in the <UI>3rd Party Plugin</UI> configuration.

    -   Translation of Escalation has been applied in the Korean website.
    -   Modified for validation after changing options.
    -   Modified to change the color of the button if you do not save the options after modification.

### `Container`

-   <Status>Change</Status> Improved the UI and content in <UI>Management</UI> > <UI>Agent Installation</UI>.

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, fixed the problem where partial matched containers are not displayed when <UI>Include</UI> is selected as the <UI>Filter</UI> option.

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, fixed the problem where the options are not reset when <img src='/img/ico-reset.svg' alt='아이콘' /> Reset is clicked.

### `Database`

#### Database commons

-   <Status>Feature</Status> In <UI>STAT/REPORT</UI> > <UI>SQL Statistics</UI>, added the following table functions.

    -   Can resize the table columns.
    -   Modified to save the setting so that the adjusted column size is not released even if you leave the page.
    -   Selecting each column in the table header can sort the list based on the selection criteria.

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, modified the location of the guide.

-   <Status>Fixed</Status> In <UI>Alert</UI> > <UI>File Log Alert</UI>, fixed the problem where previous logs remain when searching for a time zone where no log occurred.

#### PostgreSQL, MySQL

<p><Status>Feature</Status> In <UI>STAT/REPORT</UI> > <UI>Database Size</UI>, a new UI has been applied.</p>

#### Tibero

<p><Status>Change</Status> In <UI>STAT/REPORT</UI> > <UI>Tablespace Size</UI>, changed the color of the progress bar based on the tablespace usage.</p>

### `AWS`

<p><Status>Fixed</Status> Fixed the bug where selecting <UI>Event History</UI> goes to an unrelated snapshot page.</p>

### `AWS` <sup>(CloudWatch)</sup>, `Azure`, `NCP`, `OCI`

-   <Status>Change</Status> Modified the functions and UIs to set the collection cycles for each service in the project.

-   <Status>Feature</Status> Added the filter to display only the active services.

-   <Status>Feature</Status> Added the search bar for service names.

<!-- 

아래 내용은 Beta 상태, 설치형 내용으로 docs 릴리스 노트 배포에 포함하지 않습니다.

### `DATABASE` - 설치형

#### POSTGRESQL

- `Fixed` ***PG 대시보드 > 인스턴스 모니터링***  Active Session, Transactions 위젯의 단위 삭제 <허진권>
- `Fixed` *PG **대시보드 > 인스턴스 모니터링***  액티브 세션 테이블의 필터 목록 변경 <허진권>
- `Fixed` *PG **대시보드 > 인스턴스 모니터링***  query_text 컬럼의 query 클릭 시 팝업되는 SQL 보기 모달창에 파라미터가 표시되도록 수정 <허진권>
- `Feature` ***PG 대시보드 > 인스턴스 모니터링, PG 대시보드 > 멀티 인스턴스 모니터링*** 하단 액티브 세션 테이블에 세션 킬 기능 추가 <허진권>
- `Feature` ***PG 대시보드 > 멀티 인스턴스 모니터링*** 신규페이지 적용 <원성철>
- `Feature` PG ***대시보드 → 인스턴스 목록*** 하단 테이블에 프로세스 정보 탭 추가 <허진권>
- `Fixed` PG 인스턴스 목록 페이지에서 체크박스 체크 후 멀티뷰 버튼 클릭 시 멀티 인스턴스 모니터링 신규페이지로 이동 <허진권>
- `Change` ***대시보드 > 인스턴스*** ***모니터링*** 신규 UI 화면으로 변경<원성철>
- `Change` ***분석 > 락트리*** 신규 UI 화면으로 변경<원성철>
- `Feature` ***통계/보고서 > PG_STAT_STATEMENT*** 메뉴 추가 <허진권, 설치형에 옵션 적용시에만 적용>
    - Postgresql의 pg_stat_statements 뷰 정보 조회(DB에서 수행된 쿼리들에 대한 실행 통계 정보)

#### Mysql RDS - beta

- `Feature` ***대시보드 > 인스턴스 모니터링***  <원성철>
    - 락트리  ******하단 ******테이블 추가
        - DB 락일 발생한 경우 Tree Table 로 보여줍니다.
    - 프리셋 저장시 Active Session 선택한 컬럼 순서까지 저장하도록 수정
- `Feature` ***분석 > 락트리***  메뉴 추가<원성철>
    - DB 락일 발생한 경우 Tree Table 로 보여줍니다.
- `Fixed` 제품 안내 문구 수정 및 ***관리 > 에이전트 설치*** 페이지 방화벽 오픈 IP 수정 <허진권>
- `Change` ***대시보드 > 멀티 인스턴스*** ***모니터링*** 메뉴의 UI 텍스트 수정 <허진권>
- `Change` ***대시보드 > 인스턴스 목록*** 메뉴 및 ***대시보드 > 멀티 인스턴스 모니터링 메뉴의*** 기능 개선 <허진권>
    - ***대시보드 > 인스턴스 목록***  메뉴에서 ***멀티뷰*** 버튼을 클릭하면 ***신규 멀티 인스턴스 모니터링*** 페이지로 이동하도록 수정
    - ***대시보드 > 인스턴스 목록*** 메뉴와 ***대시보드 > 멀티 인스턴스 모니터링*** 메뉴 사이에 선택한 인스턴스 설정을 연동하도록 수정
    - ***대시보드 > 멀티 인스턴스 모니터링*** 메뉴에서 멀티 인스턴스 선택 및 변경사항을 저장하도록 수정

### `RUM`

#### Flex 보드

- `Fixed` 대시보드의 URL 그룹 필터가 정상 적용되지 않는 문제 수정 및 필터 위치 이동
- `Fixed` 환경별 대시보드의 브라우저별, OS별, 디바이스별 바(bar) 차트 위젯 MXQL 버그 수정
- `Fixed` 일부 위젯 중 필드 기준으로 차트 컬러가 설정되지 않는 위젯 수정

#### 분석

- `Feature` ***분석*** > ***에러 추적*** 메뉴 추가
    
    > 웹 애플리케이션 브라우저 레벨에서 발생하는 에러를 수집하여 확인할 수 있습니다.
    > 
- `Fixed` 스택 정보가 정상 표시되지 않는 문제 수정

-->

</div>
