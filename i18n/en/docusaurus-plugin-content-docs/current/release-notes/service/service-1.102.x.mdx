---
id: service-1_102_x
title: Service 1.102.X
toc_max_heading_level: 3
---

## Service 1.102.15

2023년 6월 15일

<div class='indentTab'>

### `Application`

-   <Status>Fixed</Status> <UI>분석</UI> > <UI>MSA 분석</UI> 메뉴에서 데이터를 조회하는 최대 범위를 1일 제한으로 수정

-   <Status>Fixed</Status> <UI>통계/보고서</UI> > <UI>통계</UI> > <UI>클라이언트 IP</UI> 메뉴에서 데이터를 조회하는 최대 범위를 7일 제한으로 수정

-   <Status>Fixed</Status> <UI>히트맵 트랜잭션</UI> > <UI>TX 트레이스</UI> 목록에서 <UI>통계</UI> 정보를 확인하는 경우 Trace 스텝 분석 및 메소드 팝업의 출력 데이터를 다음과 같이 수정
      
    - 각 데이터 제목 수정

    -   함수 → 메소드

    -   Parameter/Return → 파라미터/리턴 (다국어 적용)

    -   모달 제목 수정: 함수 → 메소드

    -   경과시간 단위 텍스트 수정: (ms) → ms

    -   일부 출력값 제거: 진입 CPU 시간, 메모리

-   <Status>Fixed</Status> <UI>히트맵 트랜잭션</UI> > <UI>TX 트레이스</UI> 목록에서 <UI>통계</UI> 정보를 확인하는 경우 SQL 문의 특정 키워드(content, long)가 강조되는 버그 수정

</div>

## Service 1.102.14

2023년 6월 13일

<div class='indentTab'>

<p><Status>Fixed</Status> <UI>계정 정보 관리</UI> > <UI>멀티팩터 인증</UI> > <UI>이메일로 OTP 키 보내기</UI> 버튼을 선택하면 OTP 키를 (재)생성 창이 뜨는 현상 수정</p>

</div>

## Service 1.102.13

2023년 6월 13일

<div class='indentTab'>

### `Server`

#### Alert

-   <Status>Fixed</Status> <UI>이벤트 기록</UI> 메뉴에서 <b>마지막 변경 시간</b> 컬럼 이름을 <UI>이벤트 해소 시각</UI> 으로 수정

-   <Status>Fixed</Status> <UI>이벤트 기록</UI> 메뉴에서 진행 중인 이벤트는 <UI>이벤트 해소 시각</UI> 데이터를 표시하지 않도록 수정

</div>

## Service 1.102.12

2023년 6월 12일

<div class='indentTab'>

### `Server`

<p><Status>Fixed</Status> <UI>서버 목록</UI> 메뉴에서 설치된 에이전트가 없는 경우 로딩 이미지가 계속 표시되는 버그 수정</p>

</div>

## Service 1.102.11

2023년 6월 12일

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> <UI>경고 알림</UI> > <UI>이벤트 수신 설정</UI> 메뉴에서 <UI>사용자별 이벤트 수신 설정</UI> 테이블의 펼침 기능이 작동하지 않는 버그 수정</p>

* * *

### `Application`

-   <Status>Fixed</Status> <b>트랜잭션 상세 정보</b> 창에서 <UI>SQL 요약</UI>, <UI>HTTP 호출 요약</UI>, <UI>메소드 요약</UI> 탭의 과거 데이터 조회 버그 수정

* * *

### `Server`

-   <Status>Feature</Status> 데이터 조회 시간 선택 기능 추가

</div>

## Service 1.102.10

June 9, 2023

<div class='indentTab'>

### `Common`

<p><Status>Feature</Status> <UI>계정 정보</UI> 메뉴에서 <UI>마케팅 수신 동의</UI> 항목 추가</p>

### `Server`

<p><Status>Fixed</Status> <UI>분석</UI> > <UI>메트릭스 차트</UI> 메뉴에서 에이전트 이름이 중복된 경우 CSV 파일에서 데이터가 제대로 표시되지 않는 버그 수정</p>

</div>

## Service 1.102.09

June 8, 2023

<div class='indentTab'>

<p><Status>Feature</Status> In <UI>Alert</UI> > <UI>Event Setting</UI> > <UI>Metrics</UI>, added the tooltip where the users with only the read role for alert settings can see the lists of <UI>metrics</UI> and <UI>composite metrics</UI>.</p>

</div>

## Service 1.102.08

June 8, 2023

<div class='indentTab'>

### `Log`

<p><Status>Change</Status> Modified the message that appears upon failure of parser simulation.</p>

</div>

## Service 1.102.06

May 31, 2023

<div class='indentTab'>

### `Log`

<p><Status>Fixed</Status> When adding or modifying <UI>Alert</UI> > <UI>Event Setting</UI> > <UI>Log</UI> > <UI>Real-time Log Event</UI>/<UI>Composite Log Event</UI>, fixed the error of validation logic.</p>

</div>

## Service 1.102.05

May 30, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Install</UI>, fixed the bug where the Issue a license button is reactivated when switched to another tab menu after issuing a license.</p>

</div>

## Service 1.102.04

May 26, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, fixed the display of wrong guidance message upon data inquiry failure when using <UI>Log</UI> > <UI>Log Search</UI> in the summary view of the container without any application agent.</p>

</div>

## Service 1.102.03

May 25, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, added a guidance message upon data inquiry failure when using <UI>Log</UI> > <UI>Log Search</UI> in the summary view of the container.</p>

</div>

## Service 1.102.02

May 25, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Member</UI>, fixed the FLEX_UPDATE text exposure issue in the <UI>Role</UI> list upon creation of a new project.</p>

* * *

### `Log`

<p><Status>Fixed</Status> In <UI>Log</UI> > <UI>Log Setting</UI>, modified not to allow any space in the input field when adding or modifying <UI>Log Parser</UI>, <UI>Fast Index</UI>, and <UI>Long-term Archive Statistics</UI>.</p>

</div>

## Service 1.102.01

May 25, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In Home screen > <UI>Flex Board</UI>, modified the logic to display only the product suite dashboard templates owned by the user account.</p>

</div>

## Service 1.102.00

May 24, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> Among sidebar menus, modified to add <code>Browser</code> types in <img src='/img/ico-filter.svg' alt='필터 아이콘'/> project type filters and display the text according to the selected language.

-   <Status>Change</Status> In <UI>Sitemap</UI> > <UI>User Behavior</UI>, modified to search the user behaviors for all accounts with the member management role.

-   <Status>Change</Status> Modified the Delete icon (<img src='/img/ico-trash-new.svg' alt='icon'/>) and the Channel Inquiry icon (<img src='/img/ic-message-filled.svg' alt='icon'/>) in all services.

#### Flex Board

-   <Status>Change</Status> Modified the widget's interaction button state not to be changed regardless of the side menu On/Off under Dashboard.

-   <Status>Fixed</Status> In case of the rich text widgets, fixed the bug where the saved content are not applied after modification.

-   <Status>Fixed</Status> If the browser window has a specific size, when the JSON editing window of the widget is imported in <UI>Manager</UI> mode under <UI>Flex Board</UI>, a scroll appears and disappears repeatedly in the text box.

-   <Status>Change</Status> Modified to display the specific reason when there is no data response in the following widgets.

    -   Log 4XX, 5XX Count
    -   Request Success Rate
    -   Response Time by Log

-   <Status>Change</Status> Updated the MXQL path versions of the widgets used in the <b>Integrated Service Dashboard</b> template.

-   <Status>Fixed</Status> Fixed the widget MXQL in the <b>Server Resource Plus</b> dashboard.

-   <Status>Change</Status> Added and reorganized the <UI>Log RPS Trend</UI> widget metric for the <b>Log RPS Board</b> dashboard template.

#### Management

<p><Status>Fixed</Status> In <UI>Project Member</UI>, fixed the issue where the <b>Edit Log</b> role does not appear in the <b>Role</b> column.</p>

#### My Usage

-   <Status>Fixed</Status> In <UI>Preview Invoice</UI>, fixed the bug where the discount amount of a cloud product does not appear in the <UI>Discount</UI> column.

-   <Status>Fixed</Status> In <UI>Preview Invoice</UI>, added the guidance message for billing currency when the fixed price and discount amount appear in <UI>Discount Information</UI>.

-   <Status>Change</Status> In <UI>Usage</UI>, applied UI improvements for better user experiences.

-   <Status>Feature</Status> In <UI>Usage</UI>, added the function to search the project name.

-   <Status>Change</Status> In <UI>Usage</UI>, modified to provide the average usages for each time.

* * *

### `Log`

-   <Status>Feature</Status> The <b>Edit Log</b> role has been added and the related functions has been applied to <UI>Log Setting</UI>.

-   <Status>Change</Status> In <UI>Log Setting</UI>, improved UI/UX for better user experiences.

-   <Status>Change</Status> In <UI>Log Setting</UI>, unified the mixed terms.

-   <Status>Change</Status> In <UI>Log Search</UI>, modified to enter the value, `none` as the key when adding a category while only a key has been entered in the filter with no value assigned.

-   <Status>Change</Status> In <UI>Live Tail</UI>, fixed a typo error (for the term, `spacing`) among the terms in the content filter tooltips.

-   <Status>Change</Status> In <UI>Log Search</UI>, modified not to automatically attach the prefix, `+` when `-` has been entered as the search value's prefix upon entry of a filter.

* * *

### `Database`

#### Dashboard

-   <Status>Change</Status> In <UI>Multiple Instance Monitoring</UI>, changed the selection option from <b>Database</b> to <UI>Instance</UI>.

-   <Status>Fixed</Status> In <UI>Instance List</UI>, added the function to align the table and adjust the column width to compensate for text truncation in the <UI>Event</UI> window that appears by selecting the <b>Fatal</b> or <b>Warning</b> icon in the <UI>Status</UI> column.

#### STAT/REPORT

-   <Status>Change</Status> In <UI>Table Size Increase</UI>, changed the <UI>Download CSV</UI> button to <img src='/img/ic-csv-download-ms.svg' alt='CSV 다운로드 아이콘' />.

-   <Status>Fixed</Status> In the follow menu path, fixed the problem where the download feature does not work depending on the region clicked on the drop-down menu item when the <img src='/img/ic-csv-download-ms.svg' alt='CSV 다운로드 아이콘' /> button is selected.

    -   <UI>STAT/REPORT</UI> > <UI>Table Size Increase</UI>
    -   <UI>STAT/REPORT</UI> > <UI>SQL Statistics</UI> > <UI>DB</UI>, <UI>USER</UI>, <UI>HOST</UI> tab

* * *

### `Container`

#### Container Map

-   <Status>Feature</Status> The metrics whose unit is percentage among the chart tooltips that appear when user hovers the mouse over a container element, are displayed with the bar graph charts.

-   <Status>Change</Status> Added the <UI>selected metrics</UI> item in the <img src='/img/ico-setting.svg' alt='설정 아이콘'/> > <UI>Label</UI> option.

-   <Status>Fixed</Status> Fixed the symptom where part of the <UI>Log Search</UI> and <UI>Event</UI> tabs in the summary view panel.

-   <Status>Change</Status> Modified to search logs based on the <code>microOid</code> when using the <UI>Log</UI> > <UI>Log Search</UI> function on the summary view panel in case of the app-related category.

-   <Status>Fixed</Status> When using <UI>Log</UI> > <UI>Log Search</UI> after selecting a group, fixed the bug where the app-related category logs are not searched.

* * *

### `Browser`

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Browser Error</UI>, fixed the flickering symptom while loading charts in the <UI>error message table</UI>.

-   <Status>Feature</Status> In <UI>Alert</UI> > <UI>Event Setting</UI> > <UI>Metrics</UI>, added the templates for metrics and composite metrics alert.

* * *

### `CloudWatch`, `AZURE`, `OCI`, `NCP`

#### Common

<p><Status>Feature</Status> In <UI>Management</UI> > <UI>Agent CONFIG.</UI>, added <b>1 minute</b> as a collection cycle option.</p>

#### CloudWatch

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent CONFIG.</UI>, modified the default value for validity period to <b>1 Hour</b>.</p>

</div>
