---
id: service-1_100_x
title: Service 1.100.X
toc_max_heading_level: 3
---

## Service 1.100.22

2023년 5월 23일

<div class='indentTab'>

<p><Status>Fixed</Status> <UI>분석</UI> > <UI>트랜잭션 검색</UI> 메뉴에서 <UI>경과 시간</UI> 필터 입력창에 최소 경과 시간 및 최대 경과 시간 텍스트가 표시되도록 수정</p>

</div>

## Service 1.100.21

2023년 5월 23일

<div class='indentTab'>

<p><Status>Fixed</Status> <UI>에이전트 설치</UI> 페이지의 중국어 버전 누락 버그 수정</p>

</div>

## Service 1.100.19

May 19, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Preview Invoice</UI>, fixed the bug where the usage fee does not appear when there is no discount.

-   <Status>Fixed</Status> Fixed the problem where the previous page does not appear because <code>org=undefined</code> has been entered in the query parameter when user clicks the profile icon, goes to <UI>Manage Account INFO.</UI>, and then tries to go to the previous page (by clicking Back).

* * *

### `Log`

-   <Status>Change</Status> In <UI>Log</UI> > <UI>Log Search</UI>, modified to enter the value, `none` as the key when adding a category while only a key has been entered in the filter with no value assigned.

</div>

## Service 1.100.18

May 18, 2023

<div class='indentTab'>

### `Database`

<p><Status>Fixed</Status> Added the missing `sql_param(query_param)` column in the lock tree table from the panel at the bottom of the following menus. </p>

-   **_Dashboard_** > **_Instance Monitoring_**
-   **_Dashboard_** > **_Multiple Instance Monitoring_**
-   **_Analysis_** > **_Counts Trend_**

</div>

## Service 1.100.17

May 17, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Project Subscription</UI>, fixed the bug where a whiteout occurs.</p>

</div>

## Service 1.100.16

May 16, 2023

<div class='indentTab'>

### `Application`

<p><Status>Change</Status> In <UI>Dashboard</UI> > <UI>Transaction Map</UI>, as the <UI>TX Trace</UI> table columns, added the items for <UI>SQL Count</UI>, <UI>Login ID</UI>, and <UI>SQL Fetch Count</UI>.</p>

</div>

## Service 1.100.15

May 12, 2023

<div class='indentTab'>

### `Database`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent List</UI>, fixed the error where set instance <UI>alias</UI> is not applied to the following menu.</p>

-   **_Dashboard_** > **_Instance List_**

-   **_Dashboard_** > **_Multiple Instance Monitoring_**

-   **_Analysis_** > **_Counts Trend Comparison_**

</div>

## Service 1.100.14

May 11, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> While moving the tab in <UI>STAT/REPORT</UI> > <UI>Statistics</UI>, fixed the problem where the parameters in the selected organization are kept.</p>

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Resource</UI> > <UI>Node Details</UI>, fixed the bug where partial data in the container list does not appear.</p>

</div>

## Service 1.100.12

May 10, 2023

<div class='indentTab'>

### `Container`

<p><Status>Deprecate</Status> In <UI>Dashboard</UI> > <UI>Application Service Dashboard</UI>, deleted the <UI>Dashboard for Service</UI> button for each group panel under the <UI>Multi Service Dashboard</UI> tab.</p>

</div>

## Service 1.100.11

May 10, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In Home > <UI>Flex Board</UI>, fixed the bug where the slow query widget does not appear.</p>

</div>

## Service 1.100.10

May 9, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Container Map</UI>, fixed the bug where the user without <b>Log Read</b> role cannot see the <UI>Container Stdout/Stderr</UI> data.</p>

</div>

## Service 1.100.9

May 9, 2023

<div class='indentTab'>

-   <Status>Fixed</Status> Through Home > <UI>Flex Board</UI> > <UI>Hitmap</UI> widget, fixed the problem where the member with the <b>Log Read</b> role cannot see the <UI>Transaction Log</UI> tab in the <b>Transaction Detail</b> screen.

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Member</UI>, fixed the problem where the icon button on the utmost right is hidden when the width of the browser screen is a specific size.

</div>

## Service 1.100.8

May 4, 2023

<div class='indentTab'>

### `Server`

-   <Status>Fixed</Status> In <UI>Server Setting</UI> > <UI>Server List</UI>, deleted the inactive server deletion feature.

-   <Status>Fixed</Status> In <UI>Server Setting</UI> > <UI>Server Environment Variable</UI>, deleted the inactive server deletion feature.

</div>

## Service 1.100.7

May 4, 2023

<div class='indentTab'>

### `Common`

<p><Status>Change</Status> Removed the <UI>Integrated Dashboard</UI> and <UI>Integrated Metrics Board</UI> from the side menu and modified to access them only through <UI>Sitemap</UI>.</p>

### `Log`

-   <Status>Fixed</Status> Fixed the typo error in <UI>Log</UI> > <UI>Log Setting</UI> > <UI>Log Parser Setting</UI> > <UI>Input Filter</UI> > Filter Key (okineName → okindName).

-   <Status>Fixed</Status> In <UI>Log</UI> > <UI>Live Tail</UI>, fixed the problem where the previous log appears when there is no log data searched.

</div>

## Service 1.100.6

May 3, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Management</UI>, fixed the bug where the panel title color does not change depending on the theme set by the user.</p>

</div>

## Service 1.100.5

April 28, 2023

<div class='indentTab'>

### `Log`

<p><Status>Fixed</Status> In <UI>Log</UI> > <UI>Live Tail</UI>, fixed the bug where logs were searched for all categories rather than the selected category when initially searching for log data.</p>

</div>

## Service 1.100.4

April 28, 2023

<div class='indentTab'>

### `Log`

<p><Status>Fixed</Status> Fixed the bug where logs are not queried intermittently when searching log profiles.</p>

* * *

### `Application`

<p><Status>Fixed</Status> In <UI>STAT/REPORT</UI> <UI>Statistics</UI>, fixed the problem where a whiteout occurs when the <UI>Domain</UI> tab is accessed.</p>

</div>

## Service 1.100.3

April 28, 2023

<div class='indentTab'>

### `Application`

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Transaction Search</UI>, applied the numeric type validation logic for the options that require numeric data only.</p>

</div>

## Service 1.100.2

April 27, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Preview Invoice</UI>, modified not to display the host column data in case of <b>Browser</b>, <b>Log</b>, <b>Cloud</b>, and <b>Amazon ECS</b> products.</p>

* * *

### `Application`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI> of a <b>Java</b> project, fixed the typo error for the agent download command.</p>

* * *

### `Database`

#### DB commons

<p><Status>Change</Status> Hid a menu: <UI>Analysis</UI> > <UI>Metric Anomaly Detection</UI>.</p>

#### POSTGRESQL

-   <Status>Change</Status> Hid <UI>STAT/REPORT</UI> > <UI>PG_STAT_STATEMENT</UI>.

-   <Status>Fixed</Status> Fixed the problem where a blink page appears when the guide link is clicked in <UI>Dashboard</UI> > <UI>Slow Query</UI>.

#### MYSQL

<p><Status>Fixed</Status> Fixed the problem where a blink page appears when the guide link is clicked in <UI>Dashboard</UI> > <UI>Slow Query</UI>.</p>

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, fixed the collection server IP display error in <UI>Precautions</UI>.</p>

* * *

### `ECS`

<p><Status>Fixed</Status> In <UI>Flex Board</UI>, <UI>Amazon ECS</UI> > <UI>Service</UI>, fixed the problem where only one service is displayed in the <UI>ecs_service</UI> widget.</p>

</div>

## Service 1.100.1

April 26, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>Log</UI> > <UI>Live Tail</UI>, fixed the problem where data is not output when initially searching for log data.</p>

* * *

### `Browser`

<p><Status>Fixed</Status> In <UI>Project list</UI>, fixed the problem where a <code>Browser</code> project is not searched when switched to <UI>List View</UI> mode.</p>

</div>

## Service 1.100.0

April 26, 2023

<div class='indentTab'>

### `Browser`

<p><Status>New</Status> Open the browser monitoring service. Thanks for your use. For more information about Browser Monitoring, see <a href='../../browser'>the following</a>.</p>

* * *

### `Common`

#### Search

-   <Status>Change</Status> Query is performed by pressing Enter after entering the <UI>Sidebar</UI> > <UI>Search</UI> keyword.

-   <Status>Fixed</Status> Fixed the screen tearing symptom when <UI>Sidebar</UI> > <UI>Search</UI> > <UI>Agent Search</UI> has been performed.

#### Flex Board

-   <Status>Feature</Status> In <UI>Flex Board</UI>, added the data merge summation function of the secondary chart as the chart setting option for series widgets.

-   <Status>Fixed</Status> Modified to display the date on the X-axis when searching the data for more than 3 days through the Series widget.

#### Analytics

<p><Status>Fixed</Status> In <UI>Metrics Chart</UI>, fixed the CSV download bug for the overlay chart.</p>

#### STAT/REPORT

-   <Status>Feature</Status> In <UI>Integrated Report</UI>, added the function to select the next day as the end time upon creating the <UI>Daily HTTP Call Delay Report</UI>.

-   <Status>Fixed</Status> In <UI>STAT/REPORT</UI> > <UI>REPORT</UI>, fixed the bug where the <UI>Filter</UI> option fetches the list of agents only based on the current time rather than the start date selected by the user.

#### Log

-   <Status>Change</Status> In <UI>Log Setting</UI>, modified the agent support versions for each product.

-   <Status>Fixed</Status> In <UI>Live Tail</UI>, modified to display the loading animation upon initial data loading.

-   <Status>Change</Status> In <UI>Log Setting</UI>, changed to represent the actual trial days remaining.

-   <Status>Feature</Status> In <UI>Log Search</UI>, modified to display the number of logs matched among all searches, if there are two or less search keys.

-   <Status>Feature</Status> In <UI>Log Trend</UI>, modified to display the searched logs.

-   <Status>Feature</Status> In <UI>Log Setting</UI>, added the <code>Go</code> agent configuration guide.

#### Alert

-   <Status>Change</Status> In <UI>Notification Setting</UI>, changed the name of the <b>Escalation</b> option to <UI>Repeat Notification</UI>.

-   <Status>Feature</Status> In <UI>Notification Setting</UI>, added the <UI>Repeat Notification</UI> option in the <UI>Event Notification Settings Per-User</UI> and <UI>3rd Party Plugin</UI> sections.

-   <Status>Fixed</Status> In <UI>Event Setting</UI> > <UI>Metrics</UI>, fixed the problem where a whiteout occurs when a project is initially created.

-   <Status>Fixed</Status> Fixed the validation logic bug in <UI>Composite Metrics Event</UI>.

-   <Status>Fixed</Status> In <UI>Event Setting</UI> > <UI>Metrics</UI>, fixed the problem where a text rolling occurs when reducing the browser screen size.

#### My Usage

-   <Status>Change</Status> In <UI>Preview Invoice</UI>, added the <UI>Discount Amount</UI> column in the billing history table.

-   <Status>Change</Status> In <UI>Billing and payment history</UI>, deleted the <b>product type</b> column from the list in <UI>Display Billing History</UI>.

#### Management

-   <Status>Fixed</Status> Through <UI>Manage Account INFO.</UI> <UI>Withdrawal</UI>, modified to check the membership withdrawal information (unpaid amount, granted roles).

-   <Status>Fixed</Status> Fixed the bug where the loading message is not displayed when clicking the button on the project management page.

-   <Status>Change</Status> If the project is in a restricted state, modified for a member of the project to access <UI>Management</UI> > <UI>Project Member</UI>.

-   <Status>Fixed</Status> In Select Account Icon > <UI>Manage Account INFO.</UI> > <UI>My Profile</UI> on the upper right of the screen, modified to display the unpaid amount and management support team's phone number during membership withdrawal.

#### MXQL

<p><Status>Fixed</Status> Fixed the problem where CSV is not downloaded after MXQL data query.</p>

* * *

### `Application`

-   <Status>Change</Status> Improved the design of the installation guide section in <UI>Management</UI> > <UI>Agent Installation</UI>.

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Application Dashboard</UI>, fixed the bug where the metric is hidden because the text in the summary widget does not wrap lines when the language is set to Japanese.

-   <Status>Feature</Status> In <UI>Analysis</UI> > <UI>HITMAP</UI>, added the following information on the <UI>Statistics</UI> pop-up window displayed by clicking the transaction list.

    -   In case of traces of `HttpCall` type, if there is driver information collected, **_driver info_** appears instead of **Http Call**.

    -   Additional SID values appear in the detailed information window that appears by clicking traces of `SQL` or `DBC` type.

-   <Status>Change</Status> In <UI>Analysis</UI> > <UI>Hitmap</UI> > <UI>Tx Trace List</UI>, added the SQL query copy button at the bottom when searching SQL steps in the <UI>Statistics</UI> window by selecting a transaction.

-   <Status>Fixed</Status> In <UI>STAT/REPORT</UI> > <UI>Statistics</UI>, fixed the bug where some <UI>caller agent types</UI> and <UI>caller URL</UI> columns are missing when searching for <UI>Caller</UI>.

-   <Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Multiple Transaction Trace</UI>, fixed the bug where not entering the <UI>MTID / CUSTID</UI> value does not search data.

-   <Status>Change</Status> In <UI>Analysis</UI> > <UI>Multiple Transaction Trace</UI>, added the validation logic for the <UI>MTID / CUSTID</UI> search value.

* * *

### `Server`

-   <Status>Fixed</Status> In <UI>Server List</UI> > <UI>Server Detail</UI>, fixed a missing <UI>Grouping</UI> option role checking logic for <UI>process</UI> widgets.

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Compound Eye</UI>, fixed the bug where entering a number of 24 digits or more in <UI>Refresh Interval (sec)</UI> does not allow to draw Topology View.

* * *

### `Container`

-   <Status>Feature</Status> <UI>Dashboard</UI> > <UI>Container Map</UI> > <img src='/img/ico-setting.svg' alt='설정 아이콘'/> button > Add <UI>Show Container created within 1 minute</UI> option: The containers that have been created within 1 minute are displayed in bright colors.

* * *

### `CloudWatch`, `NCP`, `OCI`, `Azure`

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, added the descriptions and modified UIs.

    -   Added a description at the bottom of the loading image displayed when entering the agent installation page immediately after a project creation.

    -   Added a description at the bottom of the loading image displayed when searching and saving the service list.

    -   Modified the height of the description so that the `CloudWatch` installation guide page appears immediately without being covered by the scroll.

* * *

### `OCI`, `Cloud Monitoring`

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, disabled the <b>RSA Public Key</b> generation button when you do not have the role to modify the OCI project.

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Cloud Monitoring</UI> > <UI>Oracle Cloud Monitor</UI> tab, fixed the bug where once the <b>RSA Public Key</b> has been generated, it is classified as registered even if authentication has not been made.

* * *

### `ECS`

<p><Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Project Subscription</UI>, fixed the bug where a whiteout occurs.</p>

</div>
