---
id: service-1_104_x
title: Service 1.104.X
toc_max_heading_level: 2
---

## Service 1.104.11

2023년 7월 7일

<div class='indentTab'>

### `Server`

<p><Status>Fixed</Status> <UI>경고 알림</UI> > <UI>이벤트 설정</UI> 메뉴에서 <UI>알림 규칙 테스트</UI> 차트로 마우스 호버 시 동시에 여러 차트의 데이터가 표시되도록 수정</p>

* * *

### `Container`

<p><Status>Feature</Status> <UI>애플리케이션 에이전트 설치</UI> 메뉴에 <b>Docker PHP</b> 항목 추가</p>

</div>

## Service 1.104.10

2023년 7월 6일

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> <UI>Flex 보드</UI> 메뉴에서 대시보드 즐겨찾기 기능 사용 시 사이드 바 메뉴에 추가되지 않는 현상 수정</p>

* * *

### `Application`

<p><Status>Change</Status> <UI>통계</UI>, <UI>히트맵</UI> 메뉴에서 검색 기능(<img src='/img/ico-btn-search.png' alt='검색 아이콘'/>)을 사용하면 시간 선택자의 <UI>최근 검색 시간</UI> 목록에 최근 검색 시간을 추가하도록 변경</p>

</div>

## Service 1.104.09

2023년 7월 6일

<div class='indentTab'>

<p><Status>Fixed</Status> 조직에 속한 프로젝트 메뉴에서 새로고침 시 무한 로딩 현상 수정</p>

</div>

## Service 1.104.08

2023년 7월 5일

<div class='indentTab'>

<p><Status>Change</Status> 비밀번호 유효성 체크 로직 변경</p>

-   최소 9자리의 영어 소문자 및 대문자, 숫자, 특수 문자 중 3종류 이상

-   이메일과 동일한 비밀번호 사용 불가

-   기존에 사용한 비밀번호와 동일한 비밀번호 사용 불가

-   연속된 키보드 배열이 포함된 비밀번호 사용 불가

-   연속된 알파벳이 포함된 비밀번호 사용 불가

-   연속된 숫자가 포함된 비밀번호 사용 불가

-   현재 비밀번호와 동일한 비밀번호 사용 불가

</div>

## Service 1.104.07

2023년 7월 5일

<div class='indentTab'>

<p><Status>Fixed</Status> 다음 메뉴 경로에서 <UI>Key</UI> 검색 상자에 한글자만 입력하면 검색 상자 밖으로 포커스가 벗어나는 현상 수정</p>

-   <UI>애플리케이션</UI> > <UI>인스턴스 성능 분석</UI> > <UI>에이전트 설정</UI>(구 버전)

-   <UI>관리</UI> > <UI>에이전트 설정</UI> > <UI>이전 버전</UI> > <UI>에이전트 설정</UI>(구 버전)

</div>

## Service 1.104.06

2023년 7월 5일

<div class='indentTab'>

<p><Status>Fixed</Status> <UI>Flex 보드</UI> 메뉴에서 실시간 조회 시 최초 호출을 제외한 업데이트 API 요청에 대해 60초 타임아웃 설정</p>

</div>

## Service 1.104.05

2023년 7월 4일

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> <UI>리소스</UI> > <UI>컨테이너 볼륨</UI> 메뉴에서 조회 버튼을 클릭하면 데이터가 출력되지 않고 로딩만 표시되는 버그 수정</p>

</div>

## Service 1.104.04

2023년 7월 4일

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> <UI>리소스</UI> > <UI>컨테이너 볼륨</UI> 메뉴에서 데이터가 출력되지 않고 무한 로딩되는 현상 수정</p>

</div>

## Service 1.104.03

2023년 7월 3일

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> 프로젝트 생성 후 자동으로 새로고침되지 않는 현상 수정</p>

* * *

### `Application`

-   <Status>Fixed</Status> Python 프로젝트 > <UI>관리</UI> > <UI>에이전트 설치</UI> 메뉴에서 <UI>에이전트 다운로드</UI> 섹션의 다운로드 명령어 수정

-   <Status>Fixed</Status> Java 프로젝트 > <UI>관리</UI> > <UI>에이전트 설치</UI> 메뉴에서 <UI>JVM 옵션 추가</UI> 섹션의 JVM 옵션 코드 수정

-   <Status>Fixed</Status> Node.js 프로젝트 > <UI>관리</UI> > <UI>에이전트 설치</UI> 메뉴에서 Node.js 지원 버전 문구 수정

* * *

### `Server`

-   <Status>Fixed</Status> <UI>서버 목록</UI> 메뉴에서 비교할 서버 선택하면 <UI>기간 조회</UI> 버튼이 활성화되지 않는 버그 수정

-   <Status>Fixed</Status> <UI>경고 알림</UI> > <UI>이벤트 기록</UI> 메뉴에서 <UI>처리내역</UI> 추가 시 이벤트 ID가 DB에 저장되지 않는 버그 수정

</div>

## Service 1.104.02

June 29, 2023

<div class='indentTab'>

-   <Status>Fixed</Status> Fixed the bug where any organization is not changed in a project.

-   <Status>Fixed</Status> In <UI>Organization Management</UI>, modified to go to the guide document in the selected language, if <UI>Go to guide document</UI> is clicked.

-   <Status>Fixed</Status> In the dashboard under <UI>Flex Board</UI>, modified to check the boolean value in case of the table widget.

</div>

## Service 1.104.01

June 29, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> Fixed the error message displayed when 10 or more free projects are generated.

-   <Status>Fixed</Status> Fixed the bug where the time selected in the calendar on the time selection UI is not saved in the <UI>Last Time Searched</UI> list.

* * *

### `Server`

#### Alert

-   <Status>Fixed</Status> In <UI>Event setting</UI>, fixed the bug where the alert settings for inactive servers are not displayed.

-   <Status>Fixed</Status> Fixed the bug where an error message appears even though the result is a success when copying an event from <UI>Log File / Window Event</UI> to the current project in <UI>Event setting</UI>.

* * *

### `Database`

#### PostgreSQL V2

-   <Status>Fixed</Status> In <UI>STAT/REPORT</UI> > <UI>SQL statistics</UI>, fixed the bug where the <UI>Search in result</UI> function does not work.

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Instance monitoring</UI>, fixed the problem where sorting is initialized in the table at the bottom.

-   <Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Counts trend comparison</UI>, fixed the issue where the left and right scroll bar is visible only when the scroll of the table of the screen is moved to the bottom.

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Instance Monitoring</UI>, <UI>Multiple instance monitoring</UI>, modified the CSS in case the text increases longer than the width of the element where the <UI>Session Info</UI> data is located on the <UI>Session Detail</UI> window.

</div>

## Service 1.104.00

June 28, 2023

<div class='indentTab'>

### `AWS Log`

<p><Status>New</Status> The <b>AWS Log</b> monitoring service is newly released. Thanks for your use! For more information about the product, see <a href='../../aws-log/introduction'>the following</a></p>.

* * *

### `Common`

-   <Status>Fixed</Status> Fixed some incorrect translations in Japanese screens.

-   <Status>Deprecate</Status> Stopped to support Chinese among multilingual services.

#### Sidebar

-   <Status>Feature</Status> Added the tooltip that displays the full project name in the project name of the Sidebar.

-   <Status>Change</Status> Changed the <code>AWS Log</code> project icon in the sidebar.

#### Dashboard

-   <Status>Change</Status> Modified the series-chart design.

    | Before modification                             | After modification                            |
    | ----------------------------------------------- | --------------------------------------------- |
    | ![before](/img/release/series-chart-before.png) | ![after](/img/release/series-chart-after.png) |

-   <Status>Change</Status> Modified the hitmap button design.

    | Before modification                              | After modification                             |
    | ------------------------------------------------ | ---------------------------------------------- |
    | ![before](/img/release/hitmap-button-before.png) | ![after](/img/release/hitmap-button-after.png) |

#### Flex Board

<p><Status>Change</Status> Modified UIs to restrict the dashboard editing based on the <b>Project Flex Board Edit</b> role.</p>

#### Report

-   <Status>Feature</Status> In <UI>Integrated report</UI>, modified the time selection limit for daily reports.

    -   **Previous**: The end time can be selected up to 23:59 for the previous day.

    -   **Change**: The end time can be selected up to 1 hour before the current time (current time - 1 hour).

-   <Status>Fixed</Status> In <UI>Integrated report</UI> > <UI>Daily report</UI>, removed the automatic correction logic for the selected time when today is selected as the <UI>end time</UI>.

#### Alert

-   <Status>Feature</Status> In <UI>Notification setting</UI> > <UI>Event notification settings per-user</UI>, added the <b>WhatsApp</b> alert setting.

-   <Status>Fixed</Status> In <UI>Notification setting</UI>, set the minimum widths for email, SMS, WhatsApp, and mobile alert columns.

-   <Status>Fixed</Status> In <UI>Notification setting</UI>, modified the style to align the settings vertically to the top when checking the reception setting details for each user (selecting <img src='/img/ic-caret-right.svg' alt='더보기 아이콘'/>).

#### Management

-   <Status>Feature</Status> Added the <b>Project Flex Board Edit</b> role.

-   <Status>Fixed</Status> In <UI>Agent CONFIG.</UI>, modified the multiple application API.

-   <Status>Feature</Status> In <UI>Create Organization</UI> and <UI>Organization Management</UI>, added the logo image guide message (guide to the recommended image size, file capacity, and extension).

-   <Status>Feature</Status> In <UI>Member List</UI>, repositioned the member deletion and role modification buttons to the member name column.

-   <Status>Change</Status> In <UI>Manage account INFO.</UI>, modified the user's password setting rule to 9 characters.

-   <Status>Feature</Status> In <UI>Manage account INFO.</UI> > <UI>User phone</UI>, <UI>Alert notification settings</UI>, added the <b>WhatsApp</b> alert setting.

* * *

### `Application`

-   <Status>Fixed</Status> 최근 30초 이내의 트랜잭션 로그 데이터가 조회되지 않는 현상 수정

-   <Status>Fixed</Status> 서비스 화면의 언어가 영어로 설정된 상태에서 Python 프로젝트의 <UI>관리</UI> > <UI>에이전트 설치</UI> > <UI>Virtual Environment 활성화</UI> 섹션에서 오타 수정

* * *

### `Server`

#### Alert

-   <Status>Feature</Status> In <UI>Event setting</UI>, added the option to <UI>copy to the current project to into the </UI><UI>Copying Event Settings</UI> (<b>Copy policy</b> in the past) function.

-   <Status>Change</Status> In <UI>Event setting</UI>, modified the <UI>Copying Event Settings</UI> function as follows.

    -   **Previous**: If you duplicate an event in the target project using the same title of the copied event, it is overwritten with the last copied event.

    -   **Change**

        -   As the event title, `{Copied Event Name}(copy)` is used.
        -   If the events are duplicated in the target project, events are created as many times as the copied count.

* * *

### `Database`

#### PostgreSQL

-   <Status>New</Status> The <b>PostgreSQL V2</b> monitoring service is newly released. To check the features changed compared to the previous, see <a href='../../postgresql/monitoring-intro#new-feature'>the following</a>.

-   <Status>Change</Status> Modified to access the 3 following menus via <UI>Sitemap</UI> among <b>PostgreSQL V2</b> menus.

    -   **_Analysis_** > **_Log File Viewer_**

    -   **_Alert_** > **_File Log Alert_**

    -   **_Management_** > **_Cloud Monitoring_**

* * *

### `Container`

#### Dashboard

-   <Status>Feature</Status> In the container summary view panel in <UI>Container Map</UI>, modified to change the messages depending on the log view role and status under the <UI>Log</UI> > <UI>Log Search</UI> tab.

-   <Status>Feature</Status> In the container summary view panel under <UI>Container Map</UI>, added the <UI>Event Count Trend</UI> chart that displays the event counts under the <UI>Event</UI> tab.

-   <Status>Feature</Status> In <UI>Container map</UI>, added the <UI>Unequal</UI> filter option.

#### Resources

-   <Status>Fixed</Status> In <UI>Node details</UI>, modified the container configuration column order in the container area at the bottom.

-   <Status>Change</Status> In <UI>Container volume</UI>, added the guidance message that the list appears only when the <img src='/img/ico-search.svg' alt='검색 아이콘'/> button is selected.

-   <Status>Feature</Status> In <UI>Performance summary</UI>, added the trend charts for container counts and pod counts.

<!---->

-   <Status>Fixed</Status> In <UI>Container list</UI>, fixed the bug where the dark theme is not applied.

-   <Status>Fixed</Status> If there is no agent installed in <UI>Container list</UI>, fixed to display the guidance message that there is no agent installed without a loading animation.

#### Management

-   <Status>Fixed</Status> In <UI>Agent Installation</UI>, modified the guidance message.

-   <Status>Fixed</Status> Modified Japanese texts in <UI>Agent Installation</UI> when the language of the service screen is set to Japanese.

* * *

### `Log`

-   <Status>Feature</Status> Under <UI>Log</UI>, added the <UI>timestamp</UI> as the <UI>Add a Column</UI> item in <img src='/img/ico-setting.svg' alt='설정 아이콘'/> <UI>Table settings</UI> > <UI>Column Settings</UI>.

-   <Status>Change</Status> Modified the message that appears upon failure of parser simulation.

-   <Status>Feature</Status> In <UI>Log Search</UI>, added the function to search logs for the time zone adjacent to the selected log.

-   <Status>Feature</Status> In <UI>Log Search</UI>, <UI>Log Trend</UI>, and <UI>Transaction log</UI>, added the keyword highlighting function.

-   <Status>Fixed</Status> In <UI>Log setting</UI> > <UI>Log long-term archive statistics</UI>, modified the guidance message displayed when the <UI>Log detection condition</UI> search key is not input when adding any log detection conditions.

* * *

### `Browser`

#### Report

<p><Status>Feature</Status> Added the <UI>Report</UI> > <UI>Daily Browser Report</UI> menu.</p>

#### Dashboard

<p><Status>Feature</Status> In <UI>Browser Error</UI>, added the error details screen.</p>

#### Analytics

-   <Status>Feature</Status> In <UI>Pageload Hitmap</UI>, added the OS version and client IP in the pageload details.

-   <Status>Change</Status> In <UI>Pageload Hitmap</UI>, changed the <UI>Filter URL Group List</UI> function to <UI>Page URL Search</UI>.

-   <Status>Change</Status> In <UI>Error Trace</UI>, modified the error stack view UI.

* * *

### `Laboratory`

<p><Status>Feature</Status> In <code>Container</code> and <code>Java</code> products, added the menu: <UI>Sitemap</UI>  > <UI>Laboratory</UI> > <UI>Remote Access Status</UI>.</p>

</div>
