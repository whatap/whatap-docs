---
id: preview-1_103_x
title: Preview 1.103.X
toc_max_heading_level: 3
---

<head>
<meta name="robots" content="noindex, nofollow"/>
</head>

:::note

[In https://preview.whatap.io](https://preview.whatap.io), the following new features and enhancements can be experienced in advance. 

:::

## Preview 1.103.29

June 26, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> In <UI>Alert</UI> > <UI>Notification setting</UI>, modified the style to align the settings vertically to the top when checking the reception setting details for each user (selecting <img src='/img/ic-caret-right.svg' alt='더보기 아이콘'/>).

-   <Status>Fixed</Status> In <UI>Alert</UI> > <UI>Notification setting</UI>, set the minimum widths for email, SMS, WhatsApp, and mobile alert columns.

* * *

### `Application`

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>HITMAP</UI> > <UI>HITMAP transaction</UI> widget, applied multiple languages to the <UI>DB Trend</UI> button of the <UI>DB tracking</UI> window that appears when the <UI>DB tracking</UI> button is clicked.</p>

* * *

### `Log`

<p><Status>Feature</Status> In <UI>Log Search</UI>, <UI>Log Trend</UI>, and <UI>Transaction log</UI>, added the keyword highlighting function.</p>

* * *

### `AWS Log`

-   <Status>Change</Status> Deleted the `beta` tag from the <UI>AWS Log</UI> item on the <b>Create project</b> page.

-   <Status>Change</Status> In the <UI>Agent Installation</UI> > <UI>Install WhaTap Forwarder</UI> step, modified to display the <UI>VPC option</UI> only for the installation type.

-   <Status>Fixed</Status> Modified the <code>productType</code> parameter value of the page URL moved by selecting the <UI>Getting the access key</UI> link from <code>sms</code> to <code>VR</code>.

</div>

## Preview 1.103.28

June 23, 2023

<div class='indentTab'>

### `Database`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Instance monitoring</UI>, deleted the text <b>Save Preset</b> in the <UI>Save Preset</UI> button.</p>

</div>

## Preview 1.103.27

June 23, 2023

<div class='indentTab'>

### `Application`

<p><Status>Feature</Status> Added a trace analysis window that pops up when dragging the chart area of the hitmap widget in the Dashboard menu.</p>

-   Improved the analysis flow and usability.
    -   Improved the transaction data analysis depth.
    -   Improved the cross/comparison analysis environment.
    -   Improved the intuitiveness of the data analysis environment.
    -   Improved the data recognition, readability, and convenience.

-   Functional improvements and others
    -   Repositioned columns in the transaction list and changed the data table widths.
    -   Integrated the diagram with stack sections.
    -   Sorted the data for the TXT and SQL download files.

</div>

## Preview 1.103.26

June 22, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> In <UI>Alert</UI> > <UI>Event setting</UI> > <UI>Metrics</UI>, fixed the issue where the threshold line exceeds the chart range when running the <UI>Testing event rules</UI> function.

-   <Status>Feature</Status> The <b>WhatsApp</b> alert setting has been added in the following menus.

    -   **_Manage account INFO._** > **_User phone_**, **_Alert notification settings_**

    -   **_Alert_** > **_Notification setting_** > **_Event notification settings per-user_**

-   <Status>Change</Status> In <UI>Manage account INFO.</UI>, modified the user's password setting rule to 9 characters.

* * *

### `Server`

<p><Status>Fixed</Status> In <UI>Server List</UI> > <UI>Server list</UI>, modified the message that appears when the servers for comparison exceed 10.</p>

* * *

### `Log`

<p><Status>Feature</Status> In <UI>Log</UI> > <UI>Log Search</UI>, added the function to search logs for the time zone adjacent to the selected log.</p>

* * *

### `Browser`

#### Dashboard

<p><Status>Feature</Status> In <UI>Browser Error</UI>, added the error details screen.</p>

#### Analytics

-   <Status>Feature</Status> In <UI>Pageload Hitmap</UI>, added the OS version and client IP in the pageload details.

-   <Status>Change</Status> In <UI>Pageload Hitmap</UI>, changed the <UI>Filter URL Group List</UI> function to <UI>Page URL Search</UI>.

-   <Status>Change</Status> In <UI>Error Trace</UI>, modified the error stack view UI.

</div>

## Preview 1.103.25

June 21, 2023

<div class='indentTab'>

### `Application`

#### Go

**_Management_** > **_Agent Installation_**, the following items have been modified.

-   <Status>Fixed</Status> Changed the GO support version (1.14 → 1.17) for agent installation.

-   <Status>Fixed</Status> Fixed to include the missing backslash (\) in the script code of the <b>Download Agent</b> section in the Amazon Linux operating system.

-   <Status>Fixed</Status> Alpine linux 운영체제에서 <b>Go 라이브러리 사용</b> 섹션의 내용이 누락된 부분 수정

-   <Status>Fixed</Status> <b>Go 라이브러리 사용</b> 섹션의 스크립트 코드 내용을 모든 운영체제에 동일하게 적용

-   <Status>Feature</Status> Added the guidance message in the <b>Agent CONF.</b> section for all operating systems.

-   <Status>Feature</Status> Added the guidance message in the <b>Start Monitoring</b> section for the Alpine Linux operating system.

#### PHP

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, modified to include the missing backslash in the script code in the <UI>Amazon linux</UI> tab > <b>Download agent</b> section.

</div>

## Preview 1.103.24

June 20, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In <UI>Flex Board</UI>, fixed the bug where the organization ID is not applied when calling the dashboard list.</p>

</div>

## Preview 1.103.23

June 20, 2023

<div class='indentTab'>

### `Browser`

<p><Status>Feature</Status> Added the <UI>Report</UI> > <UI>Daily Browser Report</UI> menu.</p>

</div>

## Preview 1.103.22

June 20, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>Integrated report</UI> > <UI>Daily report</UI>, removed the automatic correction logic for the selected time when today is selected as the <UI>end time</UI>.</p>

</div>

## Preview 1.103.21

June 20, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> Modified to go to the guidance document that matches the language selected in the service when the guidance document link of the fixed menu is selected at the top.

-   <Status>Deprecate</Status> Due to the stop of Chinese language support among multilingual services, Chinese has been excluded from the language options.

-   <Status>Change</Status> Changed the <code>AWS Log</code> project icon in the sidebar.

* * *

### `Container`

<p><Status>Fixed</Status> In the <UI>Agent Installation</UI> page, changed Japanese texts.</p>

</div>

## Preview 1.103.20

June 19, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> Under <UI>Management</UI> > <UI>Agent CONFIG.</UI>, modified the bug where an error message appears even when the request for application of configuration in the <UI>Object kind default</UI> and <UI>Project default</UI> tabs is a success.</p>

* * *

### `Container`

#### Resources

-   <Status>Fixed</Status> In <UI>Container list</UI>, fixed the bug where the dark theme is not applied.

-   <Status>Fixed</Status> If there is no agent installed in <UI>Container list</UI>, fixed to display the guidance message that there is no agent installed without a loading animation.

* * *

### `Database`

#### Common

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, changed the minimum Java version to install the agent and the XOS role setting message.</p>

#### Oracle

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Instance Monitoring</UI>, modified the hash values delivered when <UI>SQL</UI> is selected in the <UI>Active sessions</UI> table at the bottom of the screen.</p>

</div>

## Preview 1.103.19

June 15, 2023

<div class='indentTab'>

### `Common`

<p><Status>Feature</Status> In <UI>Integrated report</UI>, modified the time selection limit for daily reports.</p>

-   **Previous**: The end time can be selected up to 23:59 for the previous day.

-   **Change**: The end time can be selected up to 1 hour before the current time (current time - 1 hour).

* * *

### `Server`

#### Alert

-   <Status>Feature</Status> In <UI>Event setting</UI>, added the option to <UI>copy to the current project to into the </UI><UI>Copying Event Settings</UI> (<b>Copy policy</b> in the past) function.

-   <Status>Change</Status> In <UI>Event setting</UI>, modified the <UI>Copying Event Settings</UI> function as follows.

    -   **Previous**: If you duplicate an event in the target project using the same title of the copied event, it is overwritten with the last copied event.

    -   **Change**

        -   As the event title, `{Copied Event Name}(copy)` is used.
        -   If the events are duplicated in the target project, events are created as many times as the copied count.

</div>

## Preview 1.103.18

June 15, 2023

<div class='indentTab'>

### `Application`

-   <Status>Fixed</Status> In <UI>Analysis</UI> > <UI>MSA analysis</UI>, the maximum range to query data has been modified to the limit of 1 day.

-   <Status>Fixed</Status> In <UI>STAT/REPORT</UI> > <UI>Statistics</UI> > <UI>Client IP</UI>, modified the maximum range for data query to 7 days.

-   <Status>Fixed</Status> In <UI>HITMAP transaction</UI> > <UI>TX trace</UI>, to see the <UI>statistics</UI> data, modified the output data for trace step analysis and method popup as follows:
       
    - Modified the titles for each data.

    -   Function → Method

    -   Parameter/Return → Multiple languages

    -   Modification of the modal title: Function → Method

    -   Elapsed time unit modification: (ms) → ms

    -   Deletion of part of outputs: Start point CPU time, memory

-   <Status>Fixed</Status> In <UI>HITMAP transaction</UI> > <UI>TX trace</UI> list, to see the <UI>statistics</UI>, fixed the bug where specific keywords (content, long) of SQL statements are highlighted.

</div>

## Preview 1.103.17

June 14, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> If <UI>Manage account INFO.</UI> > <UI>Multi-factor authentication</UI> > <UI>Send OTP key via email</UI> is selected, fixed the bug where the OTP key generation window appears.</p>

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, modified the guidance messages.</p>

</div>

## Preview 1.103.16

June 13, 2023

<div class='indentTab'>

### `Server`

#### Alert

-   <Status>Fixed</Status> In <UI>Event history</UI>, modified the name of the <b>Last change time</b> column to <UI>Event resolution time</UI>.

-   <Status>Fixed</Status> In <UI>Event history</UI>, the <UI>event resolution time</UI> is not displayed for active events.

### `Container`

-   <Status>Feature</Status> In <UI>Resource</UI> > <UI>Performance summary</UI>, added the trend charts for container counts and pod counts.

-   <Status>Feature</Status> In <UI>Dashboard</UI> > <UI>Container map</UI>, added the <UI>Unequal</UI> filter option.

</div>

## Preview 1.103.15

June 13, 2023

<div class='indentTab'>

### `Database`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Instance monitoring</UI>, fixed the bug where the active session table's content at the bottom is not changed every 5 seconds.</p>

</div>

## Preview 1.103.14

June 12, 2023

<div class='indentTab'>

### `Server`

<p><Status>Fixed</Status> In <UI>Server List</UI>, the loading image appears continuously when there is no installed agent.</p>

</div>

## Preview 1.103.12

June 12, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> Fixed some incorrect translations in Japanese screens.

-   <Status>Fixed</Status> In <UI>Alert</UI> > <UI>Notification setting</UI>, fixed the but where the unfolding function in the <UI>Event notification settings per-user</UI> table does not work.

* * *

### `Application`

-   <Status>Fixed</Status> In the <b>Transaction Detail</b> window, fixed the past data query bug in the tabs for <UI>SQLs</UI>, <UI>HTTP calls</UI>, and <UI>Methods</UI>.

* * *

### `Server`

-   <Status>Feature</Status> Added the data query time selection function.

</div>

## Preview 1.103.11

June 9, 2023

<div class='indentTab'>

### `Server`

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Metrics chart</UI>, fixed the bug where the data is not properly displayed in the CSV in case of a duplicated agent name.</p>

</div>

## Preview 1.103.10

June 8, 2023

<div class='indentTab'>

<p><Status>Feature</Status> In <UI>Alert</UI> > <UI>Event Setting</UI> > <UI>Metrics</UI>, added the tooltip where the users with only the read role for alert settings can see the lists of <UI>metrics</UI> and <UI>composite metrics</UI>.</p>

</div>

## Preview 1.103.09

June 8, 2023

<div class='indentTab'>

### `Common`

#### Management

-   <Status>Feature</Status> In <UI>Create Organization</UI> and <UI>Organization Management</UI>, added the logo image guide message (guide to the recommended image size, file capacity, and extension).

-   <Status>Feature</Status> In <UI>Member List</UI>, repositioned the member deletion and role modification buttons to the member name column.

-   <Status>Feature</Status> Added the <b>Project Flex Board Edit</b> role.

#### Flex Board

<p><Status>Change</Status> Modified UIs to restrict the dashboard editing based on the <b>Project Flex Board Edit</b> role.</p>

* * *

### `Database`

-   <Status>Feature</Status> In <UI>Instance Dashboard</UI> for <b>PostgreSQL V2</b> menus, added the tooltip on the metrics information if you hover your mouse over the metrics list in the widget metrics changing window.

-   <Status>Change</Status> Modified to access the 3 following menus via <UI>Sitemap</UI> among <b>PostgreSQL V2</b> menus.

    -   **_Analysis_** > **_Log File Viewer_**
    -   **_Alert_** > **_File Log Alert_**
    -   **_Management_** > **_Cloud Monitoring_**

* * *

### `Log`

<p><Status>Change</Status> Modified the message that appears upon failure of parser simulation.</p>

</div>

## Preview 1.103.08

June 5, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent CONFIG.</UI>, modified the multiple application API.</p>

* * *

### `Container`

<p><Status>Change</Status> In <UI>Resource</UI> > <UI>Container Volume</UI>, added the message that guides you the list is output only when you select ![Search icon](/img/ico-search.svg).</p>

* * *

### `Database`

<p><Status>Change</Status> Modified the log configuration role so that PostgreSQL V2 users can use the log monitoring function free of charge.</p>

</div>

## Preview 1.103.07

June 1, 2023

<div class='indentTab'>

### `Database`

<p><Status>New</Status> Opened the PostgreSQL Monitoring V2 service. To check the features changed compared to the previous, see <a href='../../postgresql/monitoring-intro#new-feature'>the following</a>.</p>

</div>

## Preview 1.103.06

June 1, 2023

<div class='indentTab'>

-   <Status>Change</Status> Modified the series-chart design.

    | Before modification                             | After modification                            |
    | ----------------------------------------------- | --------------------------------------------- |
    | ![before](/img/release/series-chart-before.png) | ![after](/img/release/series-chart-after.png) |

-   <Status>Change</Status> Modified the hitmap button design.

    | Before modification                              | After modification                             |
    | ------------------------------------------------ | ---------------------------------------------- |
    | ![before](/img/release/hitmap-button-before.png) | ![after](/img/release/hitmap-button-after.png) |

</div>

## Preview 1.103.05

May 30, 2023

<div class='indentTab'>

### `Container`

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Install</UI>, fixed the bug where the Issue a license button is reactivated when switched to another tab menu after issuing a license.

-   <Status>Fixed</Status> In <UI>Resource</UI> > <UI>Node details</UI>, modified the container configuration column order in the container area at the bottom.

</div>

## Preview 1.103.04

May 30, 2023

<div class='indentTab'>

### `Container`

-   <Status>Feature</Status> In the container summary view panel in <UI>Dashboard</UI> > <UI>Container Map</UI>, modified to change the messages depending on the log view role and status under the <UI>Log</UI> > <UI>Log Search</UI> tab.

-   <Status>Feature</Status> In the container summary view panel under <UI>Dashboard</UI> > <UI>Container Map</UI>, added the <UI>Event Count Trend</UI> chart that displays the event counts under the <UI>Event</UI> tab.

</div>

## Preview 1.103.03

May 30, 2023

<div class='indentTab'>

### `Common`

<p><Status>Feature</Status> Added the tooltip that displays the full project name in the project name of the Sidebar.</p>

* * *

### `Log`

<p><Status>Feature</Status> Under <UI>Log</UI>, added the <UI>timestamp</UI> as the <UI>Add a Column</UI> item in <img src='/img/ico-setting.svg' alt='설정 아이콘'/> <UI>Table settings</UI> > <UI>Column Settings</UI>.</p>

</div>

## Preview 1.103.02

May 26, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, fixed the display of wrong guidance message upon data inquiry failure when using <UI>Log</UI> > <UI>Log Search</UI> in the summary view of the container without any application agent.</p>

</div>

## Preview 1.103.01

May 25, 2023

<div class='indentTab'>

### `Laboratory`

<p><Status>Feature</Status> In <code>Container</code> and <code>Java</code> suites, added the menu: <UI>Sitemap</UI>  > <UI>Laboratory</UI> > <UI>Remote Access Status</UI>.</p>

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, added a guidance message upon data inquiry failure when using <UI>Log</UI> > <UI>Log Search</UI> in the summary view of the container.</p>

</div>

## Preview 1.103.00

May 25, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> In Home screen > <UI>Flex Board</UI>, modified the logic to display only the product suite dashboard templates owned by the user account.

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Member</UI>, fixed the FLEX_UPDATE text exposure issue in the <UI>Role</UI> list upon creation of a new project.

* * *

### `Log`

<p><Status>Fixed</Status> In <UI>Log</UI> > <UI>Log Setting</UI>, modified not to allow any space in the input field when adding or modifying <UI>Log Parser</UI>, <UI>Fast Index</UI>, and <UI>Long-term Archive Statistics</UI>.</p>

</div>
