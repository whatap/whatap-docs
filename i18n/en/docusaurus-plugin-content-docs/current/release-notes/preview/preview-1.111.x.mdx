---
id: preview-1_111_x
title: Preview 1.111.X
toc_max_heading_level: 2
---

<head>
<meta name="robots" content="noindex, nofollow"/>
</head>

:::note

[In https://preview.whatap.io](https://preview.whatap.io), the following new features and enhancements can be experienced in advance. 

:::

## Preview 1.111.9

October 23, 2023

<div class='indentTab'>

### `Common`

#### User Interface

-   <Status>Feature</Status> On the <cmdname sid="platform_catalog" className="uitext" /> page, the design has been reorganized for a better user experience.

-   <Status>Fixed</Status> In case the user language is Japanese, modified the product title to English on the introduction of the sidebar.

#### Flex Board

<p><Status>Fixed</Status> Deleted the Edit buttons that cannot be used in the text editor for rich text widgets.</p>

* * *

### `Container`

-   <Status>Feature</Status> Starting from node agent 1.3.7, added new metrics and provided the redesigned node list screen.

-   <Status>Fixed</Status> Fixed the issue where the project code (PCODE) appears for the project name on the namespace project screen for the members who do not have the role to view cluster projects.

-   <Status>Feature</Status> In <cmdname sid="dashboard" className="uitext" /> > <cmdname sid="containerMap" className="uitext" />, added the function for users to select application-related fields for the block color display metric.

* * *

### `Browser`

-   <Status>Fixed</Status> In case the user language is Japanese, modified the Japanese widget title in <cmdname sid="dashboard" className="uitext" /> > <cmdname sid="rumDashboard" className="uitext" />.

-   <Status>Feature</Status> Added the <cmdname sid="sitemap" className="uitext" /> > <cmdname sid="user_behavior" className="uitext" /> menu.

-   <Status>Feature</Status> Added the <cmdname sid="analysis" className="uitext" /> > <cmdname sid="ajaxAnalytics" className="uitext" /> menu.

</div>

## Preview 1.111.8

October 19, 2023

<div class='indentTab'>

### `Common`

-   <Status>Changed</Status> In <UI>Sitemap</UI> > <UI>User Behavior</UI>, deleted the columns for <UI>Request URI</UI> and <UI>METHOD</UI> in the table.

-   <Status>Fixed</Status> Fixed the issue where clicking on the product card when creating a project with an organization selected causes the selected organization to be released.

* * *

### `Application`

#### Dashboard

-   <Status>Fixed</Status> Fixed the bug in SQL variable substitution for <UI>Trace analysis</UI>.

-   <Status>Fixed</Status> Fixed the bug that prevents access to the multi-transaction view of <UI>Trace analysis</UI>.

#### Analytics

<p><Status>Fixed</Status> Added a 0.5-second delay until next search to prevent repeated pressing of <UI>Search</UI> in <UI>HITMAP</UI>.</p>

#### STAT/REPORT

<p><Status>Changed</Status> In <UI>Statistics</UI>, modified to maintain the set search time when moving tabs.</p>

* * *

### `Database`

#### Common

-   <Status>Changed</Status> In <UI>Dashboard</UI> > <UI>Instance list</UI>, modified to fill in the table according to the browser size.

-   <Status>Changed</Status> In <UI>STAT/REPORT</UI> > <UI>SQL Statistics</UI>, improved the UI design for user convenience.

    -   Added the progress bar.

    -   Added the panel in the table area.

    -   Added ChartIndex in the table sub chart.

-   <Status>Feature</Status> In <UI>Dashboard</UI> > <UI>Instance monitoring</UI>, added the button to move to the <UI>SQL statistics</UI> menu in non-real-time mode.

#### DB V2

-   <Status>Changed</Status> Fixed the issue where active sessions are missing in <UI>Multiple instance monitoring</UI>.

-   <Status>Fixed</Status> In <UI>Multiple instance monitoring</UI>, modified the query range of the <UI>SQL Elapse Map</UI> widget chart.

#### MySQL

<p><Status>Changed</Status> In <UI>Analysis</UI> > <UI>Database parameter</UI>, modified the table scroll to be created in the table.</p>

#### PostgreSQL

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Wait analysis</UI>, modified to display the session history by using the time in <UI>Wait analysis</UI> when the <UI>Session History</UI> menu appears through the detailed information window for active sessions.</p>

#### SQL Server

<p><Status>Changed</Status> Modified the <b>MS SQL Server</b> product name to <b>SQL Server</b>.</p>

* * *

### `Container`

-   <Status>Fixed</Status> Modified the indentation of the YAML file for <UI>creating additional resources in the ISTIO environment</UI> in <UI>Management</UI> > <UI>Agent installation</UI>.

-   <Status>Changed</Status> In the following menus, replaced the <UI>Trace analysis</UI> function to a new version. For the changes, see <Link to='../../reference/changes-analysis-trace' target='_blank'>the following</Link>.

    -   <UI>APP.</UI> > <UI>Multiple Service Hitmap</UI>

    -   <UI>Dashboard</UI> > <UI>Container map</UI>

-   <Status>Fixed</Status> Added a 0.5-second delay until next search to prevent repeated pressing of <UI>Search</UI> in the following menus.

    -   <UI>APP.</UI> > <UI>Hitmap</UI>

    -   <UI>APP.</UI> > <UI>Multiple Service Hitmap</UI>

* * *

### `Log`

-   <Status>Fixed</Status> Fixed the issue where the <UI>Case Sensitive</UI> option is not applied when changing the log filter values.

-   <Status>Fixed</Status> In <UI>Log setting</UI>, modified to link the log configuration guide depending on the language selected by the user.

* * *

### `Network`

<p><Status>Changed</Status> In <UI>Sitemap</UI>, modified to expose the <UI>User Behavior</UI> menu link.</p>

</div>

## Preview 1.111.7

October 16, 2023

<div class='indentTab'>

### `Common`

<p><Status>Changed</Status> Deleted real-time update animation for line charts to improve the performance and reduce bugs.</p>

* * *

### `Database`

#### MySQL

-   <Status>Feature</Status> Added the new feature description modal in <UI>Statistics</UI> > <UI>Mysql SQL Statistics</UI>.

-   <Status>Changed</Status> In <UI>Statistics</UI> > <UI>Mysql SQL Statistics</UI>, modified to display the configuration guide if data cannot be searched.

#### PostgreSQL

-   <Status>Feature</Status> In <UI>Analysis</UI> > <UI>Wait analysis</UI>, <UI>Session History</UI>, added the new feature description modal.

-   <Status>Changed</Status> In <UI>Statistics</UI> > <UI>PG SQL statistics</UI>, modified to display the configuration guide if data cannot be searched.

</div>

## Preview 1.111.6

October 16, 2023

<div class='indentTab'>

### `Common`

-   <Status>Feature</Status> Added the sidebar width adjustment function.

-   <Status>Feature</Status> In <UI>My Usage</UI> > <UI>Payment information</UI>, added to select a currency upon registration of payment information.

-   <Status>Changed</Status> In <UI>My Usage</UI> > <UI>Project subscription</UI>, <UI>Preview invoice</UI>, and <UI>Billing and payment history</UI>, deleted the exchange rate toggle.

* * *

### `Application`

<p><Status>Changed</Status> In <UI>Dashboard</UI> > <UI>Application dashboard</UI>, added the new feature description modal.</p>

</div>

## Preview 1.111.5

October 13, 2023

<div class='indentTab'>

### `Log`

<p><Status>Changed</Status> In <UI>Log setting</UI> > <UI>Log primary parser setting</UI> tab, repositioned the descriptions for reserved words that cannot be indexed.</p>

</div>

## Preview 1.111.4

October 12, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>Event configuration</UI> > <UI>Metrics</UI> tab, fixed the problem where the content does not appear when repeatedly importing the same json file.</p>

* * *

### `Server`

<p><Status>Fixed</Status> In <UI>Server list</UI> > <UI>Server details</UI>, fixed the issue where the previous metric values of the server before change appear when changing the server list with the real-time interval gauge full.</p>

* * *

### `DataBase`

#### DB V2

<p><Status>Changed</Status> In <UI>Log</UI> > <UI>Log setting</UI>, added the explanation of how to configure logs for the installed DB and AWS RDS respectively.</p>

* * *

### `Cloud Monitoring`

<p><Status>Feature</Status> In <UI>Sitemap</UI>, added the <UI>User Behavior</UI> menu.</p>

</div>

## Preview 1.111.3

October 6, 2023

<div class='indentTab'>

### `Common`

#### User Interface

<p><Status>Fixed</Status> Deleted the function to display the interval for the default items in the interval selection option.</p>

In specific categories, the default interval value appears as **(0 second)**, but the interval value applied to the chart is sometimes not 0 second. Accordingly, the time value has been deleted.

#### Notice

<p><Status>Fixed</Status> In <UI>Notice</UI>, fixed the bug where the scroll does not appear when viewing content by clicking any post.</p>

#### Dashboard

<p><Status>Changed</Status> Under the record tab in the <UI>Transaction analysis</UI> window, added the description for the mCaller data.</p>

#### Project list

<p><Status>Fixed</Status> In case of the list view in <UI>Project list</UI>, fixed the issue where the CPM project has not been deleted.</p>

* * *

### `Application`

<p><Status>Fixed</Status> In the <UI>Heap memory</UI> widget under <UI>Dashboard</UI> > <UI>Application dashboard</UI>, fixed the typo in the title of the detailed view window.</p>

* * *

### `Server`

<p><Status>Fixed</Status> In <UI>Server list</UI> > <UI>Server list</UI>, fixed the problem where the background color of the agent list does not match the actual status when searching for agents.</p>

* * *

### `Log`

-   <Status>Feature</Status> In case of adding the parser in <UI>Log</UI> > <UI>Log setting</UI> > <UI>Log primary parser setting</UI> tab, added validation for reserved words when entering the search value for <UI>Log detection condition</UI>.

-   <Status>Fixed</Status> If <UI>Search</UI> or <UI>Exclusion search</UI> is selected in the dropdown menu that appears when a tag is clicked in the log list in <UI>Log</UI> > <UI>Log search</UI>, fixed the issue where any duplicate value is included in the <UI>Filter</UI> field.

</div>

## Preview 1.111.2

October 4, 2023

<div class='indentTab'>

### `Database`

#### Instance monitoring V2

<p><Status>Changed</Status> Modified to enter the string auto-completed with changed metrics by leaving the widget title blank when changing, adding, or deleting the metrics of the selected widget.</p>

</div>

## Preview 1.111.1

September 27, 2023

<div class='indentTab'>

### `Database`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent installation</UI>, fixed the multilingual translation bug for phrases of the XOS installation directory setting.</p>

</div>

## Preview 1.111.0

September 27, 2023

<div class='indentTab'>

### `Container`

-   <Status>Fixed</Status> In **Summary** or **Display Detail** of <UI>Dashboard</UI> > <UI>Container map</UI>, modified the guide messages according to the viewability of the <UI>Log</UI>/<UI>Event</UI> tab.

-   <Status>Fixed</Status> In <UI>Management</UI> > <UI>Namespace management</UI>, a message that indicates unavailability appears if more than 10 namespace projects are created during the trial period.

</div>
