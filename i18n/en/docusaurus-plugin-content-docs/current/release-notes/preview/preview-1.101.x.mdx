---
id: preview-1_101_x
title: Preview 1.101.X
toc_max_heading_level: 3
---

<head>
<meta name="robots" content="noindex, nofollow"/>
</head>

:::note

In [https://preview.whatap.io](https://preview.whatap.io), the following new features and enhancements can be experienced in advance.

:::

## Preview 1.101.25

May 22, 2023

<div class='indentTab'>

### `Common`

<p><Status>Change</Status> Modified the Delete icon (<img src='/img/ico-trash-new.svg' alt='icon'/>) and the Channel Inquiry icon (<img src='/img/ic-message-filled.svg' alt='icon'/>) in all services.</p>

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, additionally fixed the bug where the app-related category logs cannot be viewed when using <UI>Log</UI> > <UI>Log Search</UI> after selecting a group.<a href='#1.101.22'>Related: Preview 1.101.22</a></p>

* * *

### `Browser`

<p><Status>Feature</Status> In <UI>Alert</UI> > <UI>Event Setting</UI> > <UI>Metrics</UI>, added the templates for metrics and composite metrics alert.</p>

* * *

### `CloudWatch`, `AZURE`, `OCI`, `NCP`

<p><Status>Feature</Status> In <UI>Management</UI> > <UI>Agent CONFIG.</UI>, added <b>1 minute</b> as a collection cycle option.</p>

#### CloudWatch

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent CONFIG.</UI>, modified the default value for validity period to <b>1 Hour</b>.</p>

</div>

## Preview 1.101.24

May 19, 2023

<div class='indentTab'>

### `Common`

- <Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Preview Invoice</UI>, fixed the bug where the usage fee does not appear when there is no discount.

- <Status>Fixed</Status> Fixed the problem where the previous page does not appear because <code>org=undefined</code> has been entered in the query parameter when user clicks the profile icon, goes to <UI>Manage Account INFO.</UI>, and then tries to go to the previous page (by clicking Back).

* * *

### `Application`

<p><Status>Change</Status> In <UI>Dashboard</UI> > <UI>Transaction Map</UI>, as the <UI>TX Trace</UI> table columns, added the items for <UI>SQL Count</UI>, <UI>Login ID</UI>, and <UI>SQL Fetch Count</UI>.</p>

* * *

### `Log`

- <Status>Change</Status> In <UI>Log</UI> > <UI>Log Search</UI>, modified to enter the value, `none` as the key when adding a category while only a key has been entered in the filter with no value assigned.

- <Status>Change</Status> In <UI>Log</UI> > <UI>Live Tail</UI>, fixed a typo error (for the term, `spacing`) among the terms in the content filter tooltips.

- <Status>Change</Status> In <UI>Log Search</UI>, modified not to automatically attach the prefix, `+` when `-` has been entered as the search value's prefix upon entry of a filter.

</div>

## Preview 1.101.23

May 19, 2023

<div class='indentTab'>

### `Log`

<p><Status>Change</Status> In <UI>Log</UI> > <UI>Log Setting</UI>, unified the mixed terms.</p>

* * *

### `Browser`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Browser Error</UI>, fixed the flickering symptom while loading charts in the <UI>error message table</UI>.</p>

</div>

## Preview 1.101.22{#1.101.22}

May 18, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, fixed the bug where the app-related category logs cannot be viewed when using <UI>Log</UI> > <UI>Log Search</UI> after selecting a group.</p>

</div>

## Preview 1.101.21

May 18, 2023

<div class='indentTab'>

### `Database`

- <Status>Change</Status> In <UI>Dashboard</UI> > <UI>Multiple Instance Monitoring</UI>, changed the selection option from <b>Database</b> to <UI>Instance</UI>.

- <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Instance List</UI>, added the function to align the table and adjust the column width to compensate for text truncation in the <UI>Event</UI> window that appears by selecting the <b>Fatal</b> or <b>Warning</b> icon in the <UI>Status</UI> column.

</div>

## Preview 1.101.20

May 18, 2023

<div class='indentTab'>

### `Container`

<p><Status>Change</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, modified to search logs based on the <code>microOid</code> when using the <UI>Log</UI> > <UI>Log Search</UI> function on the summary view panel in case of the app-related category.</p>

</div>

## Preview 1.101.19

May 18, 2023

<div class='indentTab'>

### `Common`

- <Status>Fixed</Status> Fixed the widget MXQL in the <b>Server Resource Plus</b> dashboard.

- <Status>Change</Status> Added and reorganized the <UI>Log RPS Trend</UI> widget metric for the <b>Log RPS Board</b> dashboard template.

* * *

### `Database`

<p><Status>Fixed</Status> Added the missing `sql_param(query_param)` column in the lock tree table from the panel at the bottom of the following menus. </p>

- **_Dashboard_** > **_Instance Monitoring_**

- **_Dashboard_** > **_Multiple Instance Monitoring_**

- **_Analysis_** > **_Counts Trend_**

</div>

## Preview 1.101.18

May 17, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Project Subscription</UI>, fixed the bug where a whiteout occurs.</p>

</div>

## Preview 1.101.17

May 16, 2023

<div class='indentTab'>

<p><Status>Change</Status> In <UI>Sitemap</UI> > <UI>User Behavior</UI>, modified to search the user behaviors for all accounts with the member management role.</p>

</div>

## Preview 1.101.16

May 16, 2023

<div class='indentTab'>

### `Common`

<p><Status>Change</Status> In <UI>My Usage</UI> > <UI>Usage</UI>, modified to provide the average usages for each time.</p>

* * *

### `Container`

<p><Status>Fixed</Status> Fixed the symptom where part of the <UI>Log Search</UI> and <UI>Event</UI> tabs in the container map's summary view panel.</p>

</div>

## Preview 1.101.15

May 11, 2023

<div class='indentTab'>

### `Common`

- <Status>Fixed</Status> Among sidebar menus, modified to add <code>Browser</code> types in <img src='/img/ico-filter.svg' alt='필터 아이콘'/> project type filters and display the text according to the selected language.

- <Status>Fixed</Status> While moving the tab in <UI>STAT/REPORT</UI> > <UI>Statistics</UI>, fixed the problem where the parameters in the selected organization are kept.

* * *

### `Database`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent List</UI>, fixed the error where set instance <UI>alias</UI> is not applied to the following menu.</p>

- **_Dashboard_** > **_Instance List_**

- **_Dashboard_** > **_Multiple Instance Monitoring_**

- **_Analysis_** > **_Counts Trend Comparison_**

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Resource</UI> > <UI>Node Details</UI>, fixed the bug where partial data in the container list does not appear.</p>

</div>

## Preview 1.101.14

May 11, 2023

<div class='indentTab'>

### `Common`

- <Status>Fixed</Status> In Home > <UI>Flex Board</UI>, fixed the bug where the slow query widget does not appear.

- <Status>Change</Status> Updated the MXQL path versions of the widgets used in the <b>Integrated Service Dashboard</b> template.

### `Container`

- <Status>Feature</Status>  In <UI>Dashboard</UI> > <UI>Container Map</UI>, the metrics whose unit is percentage among the chart tooltips that appear when user hovers the mouse over a container element, are displayed with the bar graph charts.

- <Status>Change</Status> In <UI>Dashboard</UI> > <UI>Container Map</UI>, added the <UI>selected metrics</UI> item in the <img src='/img/ico-setting.svg' alt='설정 아이콘'/> > <UI>Label</UI> option.

</div>

## Preview 1.101.13

May 10, 2023

<div class='indentTab'>

### `Container`

<p><Status>Deprecate</Status> In <UI>Dashboard</UI> > <UI>Application Service Dashboard</UI>, deleted the <UI>Dashboard for Service</UI> button for each group panel under the <UI>Multi Service Dashboard</UI> tab.</p>

</div>

## Preview 1.101.12

May 9, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Container Map</UI>, fixed the bug where the user without <b>Log Read</b> role cannot see the <UI>Container Stdout/Stderr</UI> data.</p>

</div>

## Preview 1.101.11

May 9, 2023

<div class='indentTab'>

- <Status>Fixed</Status> Through Home > <UI>Flex Board</UI> > <UI>Hitmap</UI> widget, fixed the problem where the member with the <b>Log Read</b> role cannot see the <UI>Transaction Log</UI> tab in the <b>Transaction Detail</b> screen.

- <Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Member</UI>, fixed the problem where the icon button on the utmost right is hidden when the width of the browser screen is a specific size.

</div>

## Preview 1.101.10

May 3, 2023

<div class='indentTab'>

### `Database`

- <Status>Change</Status> In <UI>STAT/REPORT</UI> > <UI>Table Size Increase</UI>, changed the <UI>Download CSV</UI> button to <img src='/img/ic-csv-download-ms.svg' alt='CSV 다운로드 아이콘' />.

- <Status>Fixed</Status> In the follow menu path, fixed the problem where the download feature does not work depending on the region clicked on the drop-down menu item when the <img src='/img/ic-csv-download-ms.svg' alt='CSV 다운로드 아이콘' /> button is selected.

  - <UI>STAT/REPORT</UI> > <UI>Table Size Increase</UI>

  - <UI>STAT/REPORT</UI> > <UI>SQL Statistics</UI> > <UI>DB</UI>, <UI>USER</UI>, <UI>HOST</UI> tab

</div>

## Preview 1.101.09

May 4, 2023

<div class='indentTab'>

### `Server`

- <Status>Fixed</Status> In <UI>Server Setting</UI> > <UI>Server List</UI>, deleted the inactive server deletion feature.

- <Status>Fixed</Status> In <UI>Server Setting</UI> > <UI>Server Environment Variable</UI>, deleted the inactive server deletion feature.

</div>

## Preview 1.101.08

May 4, 2023

<div class='indentTab'>

### `Common`

- <Status>Change</Status> Removed the <UI>Integrated Dashboard</UI> and <UI>Integrated Metrics Board</UI> from the side menu and modified to access them only through <UI>Sitemap</UI>.

- <Status>Change</Status> Modified to display the specific reason when there is no data response in the following widgets.

  - Log 4XX, 5XX Count

  - Request Success Rate

  - Response Time by Log

### `Log`

- <Status>Fixed</Status> Fixed the typo error in <UI>Log</UI> > <UI>Log Setting</UI> > <UI>Log Parser Setting</UI> > <UI>Input Filter</UI> > Filter Key (okineName → okindName).

- <Status>Fixed</Status> In <UI>Log</UI> > <UI>Live Tail</UI>, fixed the problem where the previous log appears when there is no log data searched.

</div>

## Preview 1.101.07

May 3, 2023

<div class='indentTab'>

- <Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Member</UI>, fixed the issue where the <b>Edit Log</b> role does not appear in the <b>Role</b> column.

- <Status>Fixed</Status> In <UI>Management</UI> > <UI>Project Management</UI>, fixed the bug where the panel title color does not change depending on the theme set by the user.

</div>

## Preview 1.101.06

May 2, 2023

<div class='indentTab'>

### `Log`

<p><Status>Feature</Status> The <b>Edit Log</b> role has been added and the related functions has been applied to <UI>Log Setting</UI>.</p>

</div>

## Preview 1.101.05

May 2, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> If the browser window has a specific size, when the JSON editing window of the widget is imported in <UI>Manager</UI> mode under <UI>Flex Board</UI>, a scroll appears and disappears repeatedly in the text box.</p>

</div>

## Preview 1.101.04

April 28, 2023

<div class='indentTab'>

### `Common`

- <Status>Fixed</Status> Fixed the bug where logs are not queried intermittently when searching log profiles.

- <Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Usage</UI>, UI improvements have been reapplied.

### `Application`

<p><Status>Fixed</Status> In <UI>STAT/REPORT</UI> <UI>Statistics</UI>, fixed the problem where a whiteout occurs when the <UI>Domain</UI> tab is accessed.</p>

</div>

## Preview 1.101.03

April 28, 2023

<div class='indentTab'>

### `Application`

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Transaction Search</UI>, applied the numeric type validation logic for the options that require numeric data only.</p>

</div>

## Preview 1.101.02

April 27, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, fixed the collection server IP display error in <UI>Precautions</UI>.</p>

* * *

### `ECS`

<p><Status>Fixed</Status> In <UI>Flex Board</UI>, <UI>Amazon ECS</UI> > <UI>Service</UI>, fixed the problem where only one service is displayed in the <UI>ecs_service</UI> widget.</p>

* * *

### `Application`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI> of a Java project, fixed the typo error for the agent download command.</p>

</div>

## Preview 1.101.01

April 27, 2023

<div class='indentTab'>

### `Common`

#### My Usage

- <Status>Change</Status> In <UI>Usage</UI>, applied UI improvements for better user experiences.

- <Status>Feature</Status> In <UI>Usage</UI>, added the function to search the project name.

- <Status>Fixed</Status> In <UI>My Usage</UI> > <UI>Preview Invoice</UI>, modified not to display the host column data in case of <b>Browser</b>, <b>Log</b>, <b>Cloud</b>, and <b>Amazon ECS</b> products.

#### Flex Board

- <Status>Change</Status> Modified the widget's interaction button state not to be changed regardless of the side menu On/Off under Dashboard.

- <Status>Fixed</Status> In case of the rich text widgets, fixed the bug where the saved content are not applied after modification.

* * *

### `AWS Log`

- <Status>Feature</Status> Modified for only the users with log query role to access the <UI>Live Tail</UI>, <UI>Log Search</UI>, and <UI>Log Trend</UI> menus under <UI>Dashboard</UI>.

- <Status>Change</Status> Modified for the users who do not have the role to view logs to access <UI>Alert</UI> > <UI>Event Setting</UI> (however, functions cannot be added or modified).

* * *

### `Database`

#### DB commons

<p><Status>Change</Status> Hid a menu: <UI>Analysis</UI> > <UI>Metric Anomaly Detection</UI>.</p>

#### POSTGRESQL

- <Status>Change</Status> Hid <UI>STAT/REPORT</UI> > <UI>PG_STAT_STATEMENT</UI>.

- <Status>Fixed</Status> Fixed the problem where a blink page appears when the guide link is clicked in <UI>Dashboard</UI> > <UI>Slow Query</UI>.

#### MYSQL

<p><Status>Fixed</Status> Fixed the problem where a blink page appears when the guide link is clicked in <UI>Dashboard</UI> > <UI>Slow Query</UI>.</p>

* * *

### `Container`

<p><Status>Fixed</Status> In <UI>Management</UI> > <UI>Agent Installation</UI>, fixed the collection server IP display error in <UI>Precautions</UI>.</p>

* * *

### `ECS`

<p><Status>Fixed</Status> In <UI>Flex Board</UI>, <UI>Amazon ECS</UI> > <UI>Service</UI>, fixed the problem where only one service is displayed in the <UI>ecs_service</UI> widget.</p>

</div>

## Preview 1.101.0

April 26, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> In <UI>Log</UI> > <UI>Live Tail</UI>, fixed the problem where data is not output when initially searching for log data.</p>

* * *

### `Browser`

<p><Status>Fixed</Status> In <UI>Project list</UI>, fixed the problem where a <code>Browser</code> project is not searched when switched to <UI>List View</UI> mode.</p>

</div>
