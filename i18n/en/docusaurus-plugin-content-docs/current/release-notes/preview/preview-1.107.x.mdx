---
id: preview-1_107_x
title: Preview 1.107.X
toc_max_heading_level: 2
---

<head>
<meta name="robots" content="noindex, nofollow"/>
</head>

:::note

[In https://preview.whatap.io](https://preview.whatap.io), the following new features and enhancements can be experienced in advance. 

:::

## Preview 1.107.17

August 21, 2023

<div class='indentTab'>

### `Common`

#### Dashboard

In the **_Transaction analysis_** window, modify the following.

-   <Status>Change</Status> Added the <UI>original URL</UI> and <UI>URL/TX</UI> to display data in the <UI>Record Summary</UI> tab.

-   <Status>Change</Status> Added the <UI>original URL</UI> and <UI>URL/TX</UI> to the column settings in the transaction list.

-   <Status>Change</Status> Added the <UI>client IP</UI> to the transaction information in <UI>Table view</UI> and <UI>Tree view</UI> tabs.

-   <Status>Change</Status> When <UI>Agent name</UI> is clicked in the <UI>Record summary</UI> tab, modified to display the agent information in the <UI>Boot environment</UI> menu.

-   <Status>Change</Status> Modified the English texts of the <UI>Table View</UI> tab.

    -   Memory Accumulation (byte) → MemAllocAcc (byte)

    -   CPU Accumulation (ms) → CpuTimeAcc (ms)

-   <Status>Change</Status> Modified the UI styles for the <UI>Select column</UI> function.

* * *

### `Application`

-   <Status>Fixed</Status> In <UI>STAT/REPORT</UI> > <UI>Statistics</UI> > <UI>Transaction</UI>, modified the data search range from 5 seconds to 5 minutes, if the range is less than 5 minutes.

-   <Status>Fixed</Status> In <UI>Application</UI> > <UI>Instance performance analysis</UI>, added the exceptional handling for empty data in the <UI>Agent status</UI> tab.

</div>

## Preview 1.107.16

August 21, 2023

<div class='indentTab'>

### `Common`

#### Dashboard

<p><Status>Fixed</Status> Fixed the bug where the <UI>agent name</UI> does not appear in the transaction list of the <UI>Transaction analysis</UI> window.</p>

* * *

### `Network`

<p><Status>Fixed</Status> Modified to display the BETA tags in the headers of NPM products on the project creation and project pages.</p>

</div>

## Preview 1.107.15

August 17, 2023

<div class='indentTab'>

### `Server`

-   <Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Resource board</UI>, modified the agent status widget to display fatal and warning states separately.

-   <Status>Fixed</Status> In <UI>Server list</UI>, fixed the screen flickering issue when you enter texts in the disk and network filters.

-   <Status>Fixed</Status> In <UI>Server list</UI>, fixed the bug where the filters are not applied upon refreshing after the values for disk and network filters have been entered.

* * *

### `Database`

#### MySQL

<p><Status>Change</Status> In <UI>Management</UI> > <UI>Agent installation</UI>, added the configuration example in <em>whatap.conf</em> under the <UI>Download database agent</UI> section.</p>

* * *

### `Network`

<p><Status>Fixed</Status> In <UI>Analysis</UI> > <UI>TCP session</UI>/<UI>UDP session</UI>, fixed the issue where some values for <UI>ForeignProcessType</UI> and <UI>ForeginAppName</UI> columns appear with 0 in the session information table.</p>

</div>

## Preview 1.107.14

August 17, 2023

<div class='indentTab'>

### `Network`

<p><Status>New</Status> The <b>Network Performance Monitoring</b> service is released. Thanks for your use! The service can be used in <Link to='https://preview.whatap.io' target='_blank'>https://preview.whatap.io</Link>. For more information, see <Link to='../../npm/introduction' target='_blank'>the following</Link>.</p>

* * *

### `Database`

#### Instance Monitoring V1

<p><Status>Fixed</Status> Fixed the issue where the widget chart size appears abnormally in the widget <UI>count selector</UI> modal.</p>

#### V2 commons

-   <Status>Change</Status> Modified the modal UI for the table column selector.

-   <Status>Change</Status> Modified the <UI>Session Kill</UI> button to be exposed only to the members with <b>Edit</b> role among project members.

-   <Status>Feature</Status> Applied to display maximum data values on the line chart.

#### Multiple instance monitoring V2

-   <Status>Feature</Status> Added the Info icon to display metrics for each widget.

-   <Status>Feature</Status> Separated the tabs for each product environment (RDS, XOS).

-   <Status>Change</Status> In the <UI>XOS Disk Usage</UI> widget, modified as <code>Mount Source</code> → <code>Mount Point</code> among the table columns (requires an update to the latest version of agent).

#### Database Size V2

<p><Status>Feature</Status> Added the Download CSV feature.</p>

#### PG SQL Statistics

<p><Status>Feature</Status> Added the option to select lookup criteria (temp blocks read, temp blocks written).</p>

* * *

### `Log`

-   <Status>Fixed</Status> In <UI>Log Search</UI>, modified not to display the drop-down menu when the <code>pcode</code> tag is clicked.

-   <Status>Feature</Status> In <UI>Log setting</UI>, improved the selection UI for detection of categories and logs.

</div>

## Preview 1.107.13

August 11, 2023

<div class='indentTab'>

<p><Status>Fixed</Status> In case of merging the series widget objects in <UI>Flex Board</UI>, fixed the problem where the maximum and minimum values do not work.</p>

</div>

## Preview 1.107.12

August 10, 2023

<div class='indentTab'>

### `Common`

#### Organization

<p><Status>Fixed</Status> Fixed the issue where the selected organization is disabled in the following menu path.</p>

-   In case of going to the project list in MySQL Project > <UI>Management</UI> > <UI>Agent installation</UI>

-   In case of clicking a group in the side menu in MySQL Project > <UI>Management</UI> > <UI>Agent installation</UI>

#### Dashboard

<p><Status>Fixed</Status> Under the <UI>Table</UI> tab in the <UI>Transaction analysis</UI> window, fixed the bug where the variable decryption password input is invisible in the SQL step details view modal.</p>

* * *

### `Application`

<p><Status>Fixed</Status> In <UI>Statistics</UI> > <UI>Transaction</UI>, fixed the issue where the selected organization is disabled when clicking Transaction search or Transaction trace.</p>

</div>

## Preview 1.107.11

August 10, 2023

<div class='indentTab'>

### `Container`

<p><Status>Fixed</Status> In <UI>Container map</UI>, fixed the bug where separation of tag items in the summary view into the <UI>Summary</UI> tab is missing in case of the pod category.</p>

</div>

## Preview 1.107.10

August 9, 2023

<div class='indentTab'>

### `Browser`

#### Analytics

<p>In <UI>Page Load Hitmap</UI>, the following have been modified.</p>

-   <Status>Feature</Status> In <UI>Page Load Details</UI>, added the browser error data and step column.

-   <Status>Feature</Status> In <UI>Page List</UI>, the red font color appears for page load events that include the browser error data.

-   <Status>Feature</Status> It has been set to limit the search time to the maximum of 15 days and to make it impossible to search for data older than 30 days in the past.

</div>

## Preview 1.107.09

August 8, 2023

<div class='indentTab'>

### `Container`

<p><Status>Change</Status> Separated the tag items in the summary view of <UI>Container map</UI> into the <UI>Summary</UI> tab.</p>

</div>

## Preview 1.107.08

August 8, 2023

<div class='indentTab'>

### `Database`

#### Cubrid

<p><Status>Change</Status> Under the <UI>Download Database Agent</UI> section in <UI>Management</UI> > <UI>Agent installation</UI>, the following have been modified.</p>

-   Agent file download command

-   Installation command in whatap.conf

-   Addition of the link to the guide

</div>

## Preview 1.107.07

August 8, 2023

<div class='indentTab'>

#### Project list

<p><Status>Fixed</Status> In <UI>Project list</UI>, modified the Japanese term for <UI>Application</UI> (アプリケーション → アプリ) to fix the issue where the text is cut off when displayed in an extended card view.</p>

#### Dashboard

In the **_Transaction analysis_** window, modify the following.

-   <Status>Fixed</Status> To enable the filtering in <UI>Flex Board</UI>, fixed the bug where the <UI>Transaction analysis</UI> screen does not appear.

-   <Status>Fixed</Status> In the <UI>SQL Step details</UI> modal under the <b>Table</b> tab, fixed the bug where SQL variable decryption does not work.

-   <Status>Change</Status> Modified the setting of the <UI>No</UI> column in the transaction list to be unmodifieable.

* * *

### `Log`

-   <Status>Feature</Status> Added the scroll position movement and highlighting functions to the base log from the adjacent logs.

-   <Status>Change</Status> In <UI>Alert</UI> > <UI>Event setting</UI> > <UI>Log</UI>, deleted <UI>All</UI> from <UI>Search key</UI> when newly creating a <UI>real-time log event</UI>.

</div>

## Preview 1.107.06

August 7, 2023

<div class='indentTab'>

### `Common`

#### Dashboard

-   <Status>Change</Status> In the <UI>Table view</UI> tab of the <UI>Transaction analysis</UI> window, modified the expression for trace information that corresponds to the <UI>Contents</UI> column.

    -   Before: The description is displayed in a row.

    -   After:
        -   The description can be displayed up to 4 rows.
        -   When the information is clicked, the entire content of the step appears in modal mode.

</div>

## Preview 1.107.05

August 4, 2023

<div class='indentTab'>

### `Log`

-   <Status>Fixed</Status> In <UI>Log Search</UI> and <UI>Live Tail</UI>, modified the filter operator from <code>=</code> to <code>!=</code>.

-   <Status>Fixed</Status> In <UI>Log Search</UI> and <UI>Live Tail</UI>, modified the search value that appears by entering the numeric keyword, <code>.n</code> to be displayed from <code>+>= 123</code> to <code>>= 123</code> as the <code>comparison operator</code> <code>search value</code>.

* * *

### `Database`

#### MongoDB

<p><Status>Fixed</Status> Under the table in <UI>Instance list</UI>, fixed the problem where any database item is missing.</p>

</div>

## Preview 1.107.04

August 3, 2023

<div class='indentTab'>

### `Common`

#### Dashboard

In the **_Transaction analysis_** window, modify the following.

-   <Status>Fixed</Status> Fixed the bug where multi-transaction information does not appear when entering from the <code>Container</code> platform's master project.

-   <Status>Fixed</Status> Fixed the project filtering's malfunction in <UI>Multi-transaction</UI> tab > <UI>Show chart</UI>.

-   <Status>Fixed</Status> Fixed the malfunction while sorting by the transaction ID and multi-transaction ID columns in the transaction list.

-   <Status>Fixed</Status> Fixed missing translation tooltips for button descriptions in the <UI>Tree View</UI> tab.

    -   **Display Time** → **_시간 표시_**

    -   **Hide Time** → **_시간 숨기기_**

    -   **Display Time Bar** → **_시간바 표시_**

-   <Status>Change</Status> Modified the targets to search in the transaction list.

    -   Before: Need to specify the target columns to search

    -   After: Columns displayed on the screen

* * *

### `Application`

#### Application Dashboard

<p><Status>Change</Status> Added the guide on organization of the <UI>Transaction analysis</UI> function.</p>

* * *

### `Database`

#### MySQL V2

<p><Status>Fixed</Status> In <UI>Instance monitoring</UI>, changed the metrics for the memory free (Cloudwatch) widget included in the <UI>Default(rds)</UI> preset (<code>FreeStorageSpace</code> → <code>FreeableMemory</code>).</p>

* * *

### `Log`

-   <Status>Feature</Status> In <UI>Alert</UI> > <UI>Log</UI>, the filtering is disabled when no category is selected in case of adding the template events with <UI>Composite log event</UI>.

-   <Status>Change</Status> In the highlight function under <UI>Log</UI>, changed the colors that can be selected and the log's body font.

</div>

## Preview 1.107.03

August 2, 2023

<div class='indentTab'>

### `Common`

-   <Status>Fixed</Status> In Management > Project member, deleted the <b>Member role scheme reorganization notice</b> message window.

-   <Status>Fixed</Status> In <UI>Flex Board</UI>, hided the blog content when creating a Flex Board if the language is not set to Korean.

* * *

### `Application`

In <p><UI>STAT/REPORT</UI> > <UI>Statistics</UI> > <UI>Transaction</UI> > <UI>T</UI> / <UI>U</UI> selection > <UI>Top stack of URL</UI> / <UI>Unique stack URL</UI>, the following functions have been modified.</p>

-   <Status>Fixed</Status> Modified the <UI>Time selector</UI> option to the latest version.

-   <Status>Fixed</Status> Changed the data lookup period from 62 days to 3 days.

* * *

### `Database`

#### SQL Server

<p><Status>Change</Status> In <UI>Management</UI> > <UI>Agent installation</UI>, added the configuration command example in <em>whatap.conf</em> under the <UI>Download database agent</UI> section.</p>

</div>

## Preview 1.107.02

August 1, 2023

<div class='indentTab'>

### `Database`

#### Common

<p><Status>Fixed</Status> In <UI>Dashboard</UI> > <UI>Slow query</UI>, fixed the whiteout phenomenon during slow queries.</p>

#### MySQL

<p><Status>Change</Status> Under <UI>Management</UI> > <UI>Agent installation</UI>, modified the `grant` command for collection the table data in the <UI>Create the monitoring account</UI> section.</p>

</div>

## Preview 1.107.01

August 1, 2023

<div class='indentTab'>

### `Common`

<p><Status>Fixed</Status> If an organization is selected, fixed the issue where it is deselected when the <UI>Agent Installation</UI> menu moves to the project list.</p>

* * *

### `Application`

-   <Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Stack</UI>, modified the maximum lookup period for top stacks and unique stacks from 62 days to 3 days.

-   <Status>Fixed</Status> In <UI>Analysis</UI> > <UI>Performance Trend</UI>, modified the maximum lookup period from 62 days to 31 days.

* * *

### `Container`

<p><Status>Feature</Status> In <UI>Dashboard</UI> > <UI>Container map</UI>, replaced the filter to enhance the function.</p>

</div>

## Preview 1.107.00

July 31, 2023

<div class='indentTab'>

### `Log`

<p><Status>Feature</Status> In <UI>Alert</UI> > <UI>Event Setting</UI> > <UI>Log</UI> > <UI>Composite log event</UI>, added the templates for function update and addition of the log event template.</p>

</div>
