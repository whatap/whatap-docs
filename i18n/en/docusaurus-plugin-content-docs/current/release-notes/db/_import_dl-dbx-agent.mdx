:::note 

<code class='my'>Download</code>&nbsp;<Link to={`https://repo.whatap.io/agent/dbx/dbx/whatap.agent.dbx-${props.ver}.jar`}>whatap.agent.dbx-{props.ver}</Link>

To use the agent's *JAR* file of the version, replace the *JAR* file in the existing package. Upon installation for the first time, download the *JAR* file from the link above and then replace it with a desired version. See the agent installation documents for each platform for more information.

:::
