:::note

<p><b>Download</b>: <Link to={`https://repo.whatap.io/maven/io/whatap/whatap.agent/${props.ver}/whatap.agent-${props.ver}.jar`}>Java Agent v{props.ver}</Link></p>

To install the Java agent for the first time, install the package first using the following command. For more information about agent installation, see [the following](../../java/install-agent).

```bash
wget https://api.whatap.io/agent/whatap.agent.java.tar.gz
```

:::
