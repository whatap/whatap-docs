:::note

To install the Java Batch agent for the first time, install the package first using the following command. For more information about agent installation, see [the following](../../java/add-jvm-opt/batch-app).

```bash
wget https://api.whatap.io/agent/whatap.agent.batch.tar.gz
```

:::
