---
id: k8s-1_7_0
title: Kubernetes Agent v1.7.0
toc_max_heading_level: 2
pagination_next: release-notes/k8s/k8s-1_6_1
displayed_sidebar: releaseSidebar
---

May 13, 2024

<Status>Feature</Status> Added data in <b>TagCount</b>.

- Added the `reason` and `message` fields to the `kube_pod` category.

  - `reason`: Reason of the Pod status
  - `message`: Message of the Pod status

- Added the `kube_state` (Container state) field to the `container` category.

- Collected the pending status pod and container data (`kube_pending_pod`, `kube_pending_container`).

  The collected data can be seen in <Cmdname sid="side_lab" className="uitext" /> &gt; <Cmdname sid="side_pendingPod" className="uitext" />.

- Collected the data related to the service (`kube_service`).

- Upon booting the agent, modify to send the user installation runtime and cluster runtime to the <code>#whatapEvent</code> category.

:::info

Agent download: [kube_mon_1.7.0.tar](https://repo.whatap.io/docker/kube_mon_1.7.0.tar)

:::
