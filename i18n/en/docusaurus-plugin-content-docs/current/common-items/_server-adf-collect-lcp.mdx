It collects the performance for CPU, memory, disk, and NIC in the Linux containers. The collected performance data can be checked in **_Category_** `server_container`.

```bash title='Linux Shell'
echo "container.enabled=true" |sudo tee -a /usr/whatap/infra/conf/whatap.conf
```
