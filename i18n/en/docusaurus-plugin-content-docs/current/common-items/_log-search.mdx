import Logroleview from "../common-items/_log-role-view.mdx";
import Loguimainterm from "../common-items/_log-ui-main-term.mdx";
import Logagentoption from "../common-items/_log-agent-option.mdx";
import LogCl from "../common-items/_log-table-cl-modify.mdx";
import LogDeIdentification from "../common-items/_log-de-identification-common.mdx";

<Logroleview />

<Xclude product='aws-log'>

Home &gt; Select Project &gt; <Cmdname sid="side_log" className="uitext" /> &gt; <Cmdname sid="side_logSearch" className="uitext" />

</Xclude>

<InDoc product='aws-log'>

Home &gt; Select Project &gt; <Cmdname sid="side_dashboard" className="uitext" /> &gt; <Cmdname sid="side_logSearch" className="uitext" />

</InDoc>

In <Cmdname sid="side_logSearch" className="uitext" />, you can search a large number of integrated logs under various conditions and specify the desired logs. Multiple search conditions can be specified with parsed keys and values, so only the log data that matches the desired conditions are extracted.

The searched log data is imported to the dynamic page line by line. If the scroll reaches the bottom, the next data is automatically retrieved and displayed. <Loguimainterm />

<Xclude product='postgresql,mysql,oracle,oracle-pro'>

<ImgLang img='log-search.png' desc='Log search' />

</Xclude>

<InDoc product='postgresql,mysql,oracle,oracle-pro'>

<ImgLang img='db-log-search.png' desc='' />

</InDoc>

## Request Data

- If the scroll reaches the bottom, the next data is retrieved. You can view 10,000 logs at a time.

- ![number-s](/img/number-03.png) You can see the total number of logs viewed from the upper left of the log table. 

- Log data can be queried in chronological order. On the upper right of the ![number-s](/img/number-03.png) log table, select a desired search method among <Cmdname sid="past_order" className="uitext" /> and <Cmdname sid="recent_order" className="uitext" />.

- After specifying the time range, select <Cmdname sid="apply" className="uitext" /> to finish the search time setting, and then select ![Search icon](/img/ic-search-on-press.svg) <Cmdname sid="search" className="uitext" /> to search for data.

- On the upper right of the ![number-s](/img/number-03.png) log table, select the <LinkImage img='ic-fullscreen.svg' desc='전체 화면 아이콘'/> <Cmdname sid="TLT06869" className="uitext" /> icon, you can see the <Cmdname sid="log" className="uitext" /> and <Cmdname sid="TTL06444" className="uitext" /> in the full screen.

- <Logagentoption />

  <LogCl />

## Checking the log content

import Logcontentscheck from "../common-items/_log-contents-check.mdx";

<Logcontentscheck />

## Filter{#log-search-filter}

### Apply Filter

import Logfilterapply2 from "../common-items/_log-filter-apply2.mdx";

<Logfilterapply2 />

### Applying non-parsing keyword filters

import Logfilterapplynonparsing from "../common-items/_log-filter-apply-non-parsing.mdx";

<Logfilterapplynonparsing />

### Modifying the filter

import Logfiltermodify from "../common-items/_log-filter-modify.mdx";

<Logfiltermodify />

### Search Key

import Logfiltersearchkey from "../common-items/_log-filter-search-key.mdx";

<Logfiltersearchkey />

### Filter input syntax{#filter-expressions}

import Logfilterexpressionssearchkey from "../common-items/_log-filter-expressions-search-key.mdx";

<Logfilterexpressionssearchkey />

import Logfilterexpressionscommons from "../common-items/_log-filter-expressions-commons.mdx";

<Logfilterexpressionscommons />

{/* 개인정보 비식별화 */}

<LogDeIdentification />

## Log tagging options {#log-tag-options}

When you select a log tag, the following drop-down menu appears. You can find the <Cmdname sid="search" className="uitext" />, <Cmdname sid="exclusion_search" className="uitext" />, <Cmdname sid="TTL06582" className="uitext" /> and <Cmdname sid="TTL06527" className="uitext" /> options. 

<ImgLang img='log-search-tag.png' desc='Log tag dropdown menu'/>

- Copy

- **Search**

  If you select the <Cmdname sid="search" className="uitext" /> option, the tags that correspond to <Cmdname sid="filter" className="uitext" /> are entered with the inclusion (`==`) condition.

- **Exclusion search**

  If you select the <Cmdname sid="exclusion_search" className="uitext" /> option, the tags that correspond to <Cmdname sid="filter" className="uitext" /> are entered with the exclusion (`!=`) condition.

- **Adjacent Log**

  If you select the <Cmdname sid="TTL06582" className="uitext" /> option, the <Cmdname sid="TTL06582" className="uitext" /> detail window appears. Search for the logs of the target server and the logs in time zones adjacent to the selected log. You can click <Cmdname sid="time_selector" className="uitext" /> to see the logs in adjacent time zones. Reference logs appear with a blue background.

- Transaction

  If the log contains the `@txid` and `@mtid` tags, you can see the information in the transaction Information details window by clicking the transaction information option.

## Content highlighting {#content-highlight}

import Logcontentshighlight from "../common-items/_log-contents-highlight.mdx";

<Logcontentshighlight />

## Setting the table {#set-table}

import Loguitablesetting from "../common-items/_log-ui-table-setting.mdx";

<Loguitablesetting />
