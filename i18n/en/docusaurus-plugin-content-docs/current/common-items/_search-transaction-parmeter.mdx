### Viewing SQL parameters {#transaction-parameter}

Select the SQL step in the <Cmdname sid="TTL06126" className="uitext" /> tab or select ![Popup Icon](/img/ic-popup.svg) in the <Cmdname sid="TTL06127" className="uitext" /> tab. The <UI>SQL</UI> window appears to display parameters.

<ImgLang img='sql-parameter-query.png' desc='SQL' />

<Xclude product='java,php,golang'>

- <Cmdname sid="BTN06764" className="uitext" />: You can see the decrypted parameter value. The password is a 6-digit string in the *`WHATAP_HOME`/paramkey.txt* file. You can change it to another string.

  :::note

  The key in *paramkey.txt* is required to view the SQL variables and HTTP queries, and stop the threads.

  :::

</Xclude>

<InDoc product='java'>

- <Cmdname sid="BTN06764" className="uitext" />: You can see the decrypted parameter value. The password is a 6-digit string(`paramkey`) in the *`WHATAP_HOME`/security.conf* file. You can change it to another string.

  :::note

  <Cmdname sid="BTN06764" className="uitext" /> is required to view the SQL variables and HTTP queries, and stop the threads. For more information, see [the following](install-agent#security).

  :::

</InDoc>

<InDoc product='php,golang'>

- <Cmdname sid="BTN06764" className="uitext" />: You can see the decrypted parameter value. The password is a 6-digit string(`paramkey`) in the *`WHATAP_HOME`/security.conf* file. You can change it to another string.

  :::note

  Depending on the agent version, the password configuration file may be different. Change the string in the *security.conf* file or *paramkey.txt* file.

  :::

</InDoc>

- <Cmdname sid="format" className="uitext" />: You can indent and format your SQL statements for better readability.

- ![Copy icon](/img/ico-copy.svg): A SQL statement can be copied to the clipboard.

- <Cmdname sid="statistics" className="uitext" />: Go to <Cmdname sid="side_statistics" className="uitext" /> &gt; <Cmdname sid="side_statSql" className="uitext" />.

<Xclude product='kubernetes'>

:::tip

To search SQL variables and HTTP queries, add the following options to the agent settings.

- For agent settings related to recording SQL parameters, see [the following](agent-dbsql#profile-sql-param).

- For agent settings related to recording the HTTP parameters, see [the following](agent-transaction#profile_http_parameter).

<InDoc product="php">

```ini title='whatap.conf'
# SQL parameter lookup option: If the option is applied, SQL parameters are collected after being encrypted.
whatap.profile_sql_param_enabled=true 

// HTTP parameter lookup option: If the option is applied, HTTP query parameters are collected after being encrypted.
whatap.profile_http_parameter_enabled=true 
```

</InDoc>

<Xclude product="php">

```ini title='whatap.conf'
# SQL parameter lookup option: If the option is applied, SQL parameters are collected after being encrypted.
profile_sql_param_enabled=true 

// HTTP parameter lookup option: If the option is applied, HTTP query parameters are collected after being encrypted.
profile_http_parameter_enabled=true 
```

</Xclude>

:::

</Xclude>

<InDoc product='kubernetes'>

:::tip

To search SQL variables and HTTP queries, add the following options to the agent settings. For more information about the agent settings for DB and SQL, see [the following](../java/agent-dbsql).

```ini title='whatap.conf'
# SQL parameter lookup option: If the option is applied, SQL parameters are collected after being encrypted.
profile_sql_param_enabled=true 

// HTTP parameter lookup option: If the option is applied, HTTP query parameters are collected after being encrypted.
profile_http_parameter_enabled=true 
```

:::

</InDoc>
