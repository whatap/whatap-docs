<p>The metric chart screen example displays the metrics (<code>{props.metric}</code>) collected from the monitoring targets at the selected time as a chart. At this time, the collection server uses the <code>{props.category}</code> category metric, which has the selected metric <code>{props.metric}</code> value as a field.</p>
