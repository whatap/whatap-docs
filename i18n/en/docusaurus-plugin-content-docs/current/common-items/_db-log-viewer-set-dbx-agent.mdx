### DBX Agent Setting

Set the log file path and file name to check the server where the WhaTap DBX agent has been installed. To set up multiple log files, use comma (,) as the delimiter.

```ini title='whatap.conf'
fileview={fullpath}
# fileview=$WHATAP_HOME/logs/dbx.log
```
