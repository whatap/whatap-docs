It collects the Windows service operation status. The collected service operation status can be checked in **_Category_** `server_windows_service`.

```bash title='Windows Powershell'
# The admin role is required.
Add-Content -Path "C:\Program Files\WhatapInfra\whatap.conf" -Value "winservice.enabled=true"
```
