<p>
  It is the description of the agent configuration using the AWS CloudWatch. If collection is started, the data is input in metrics and can be seen in the <code>{props.name}</code> category under <UI>Analysis</UI> > <UI>Metrics Chart</UI> as well as the other metrics.
</p>
