Through the <Cmdname sid="AA005" className="uitext" /> widget in <Cmdname sid="side_appDashboard" className="uitext" />, you can open the <Cmdname sid="TTL06209" className="uitext" /> window to check the transaction list and step information of each transaction at once. Drag the desired area on the hitmap widget. The <Cmdname sid="TTL06209" className="uitext" /> window appears that contains the transaction list and sub-steps of the selected area.

:::tip

To classify and see only the transactions with errors, select <Cmdname sid="dashboard_hitmap_error" className="uitext" /> on the upper right of the <Cmdname sid="AA005" className="uitext" /> widget in <Cmdname sid="side_appDashboard" className="uitext" /> and then drag the chart area.

<ImgLang img="apm-hitmap-error.png" desc="Hitmap widget" />

:::
