- ![number1](/img/number-01.png) Selecting a lookup target

  <InDoc product='cubrid'>

  You can select an instance for inquiry. The collected data of the selected instance is automatically applied to the ![number2](/img/number-02.png) graph chart and the ![number3](/img/number-03.png) <Cmdname sid="active_sessions" className="uitext" /> table.

  </InDoc>

  <InDoc product='oracle-v1,mssql,tibero'>

  You can select an instance and a database included in the instance for data inquiry. The collected data of the selected instance and its database are automatically applied to the ![number2](/img/number-02.png) graph chart and the ![number3](/img/number-03.png) **_Active Sessions_** table.

  </InDoc>

- ![Number 2](/img/number-02.png) Graph chart section

  The horizontal axis of each widget is time, and the vertical axis is the metric number. If you select a specific time period in each widget, <Cmdname sid="side_analysis" className="uitext" /> &gt; <Cmdname sid="side_dbTrend" className="uitext" /> appears.

<Xclude product="mssql">

- ![Number 3](/img/number-03.png) <Cmdname sid="active_sessions" className="uitext" /> | <Cmdname sid="lock_tree" className="uitext" /> | <Cmdname sid="process_info" className="uitext" />

  You can view the active session, lock tree, and process information.

  - <Cmdname sid="active_sessions" className="uitext" />

    You can view active sessions and queries. If you select an item in the rightmost <UI> sql_text </UI> or <UI>query</UI> column in the table, the <Cmdname sid="TTL06224" className="uitext" /> window appears. You can see the full text and plan information for the selected query. For more information, see [the following](#query-details).

  - <Cmdname sid="lock_tree" className="uitext" />

    You can see the lock session information, and holder and waiter information in real time. For more information about the table columns, see [the following](#column-info).

  - <Cmdname sid="process_info" className="uitext" />

    You can query the resource usage of the database server in real time.

</Xclude>

<InDoc product="mssql">

- ![Number 3](/img/number-03.png) <Cmdname sid="active_sessions" className="uitext" /> | <Cmdname sid="lock_tree" className="uitext" />

  You can view the active session, lock tree, and process information.

  - <Cmdname sid="active_sessions" className="uitext" />

    You can view active sessions and queries. If you select an item in the rightmost <UI> sql_text </UI> or <UI>query</UI> column in the table, the <Cmdname sid="TTL06224" className="uitext" /> window appears. You can see the full text and plan information for the selected query. For more information, see [the following](#query-details).

  - <Cmdname sid="lock_tree" className="uitext" />

    You can see the lock session information, and holder and waiter information in real time. For more information about the table columns, see [the following](#column-info).

</InDoc>

:::note

- Colors in the database correspond to the ones in the graph chart.

- For more information about <Cmdname sid="side_dbTrend" className="uitext" />, see [the following](analysis-count-trend).

- For more information about each metric, see [the following](metrics-data-list). 

- For more information about the columns, see [the following](#column-info).

<Xclude product="mssql">

- The <Cmdname sid="process_info" className="uitext" /> can be viewed by additionally configuring the XOS agent. For more information, see [the following](install-agent#apply-xos-agent).

</Xclude>

:::
