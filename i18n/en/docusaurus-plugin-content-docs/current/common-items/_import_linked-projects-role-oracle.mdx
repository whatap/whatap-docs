:::note 

**Role setting**

For Oracle and Oracle Pro platforms, the following permissions are required to add a linked project:

```bash 
grant select on sys.v_$session to DB access account;
# ex) grant select on sys.v_$session to petapp;
```

:::