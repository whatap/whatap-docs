<Xclude product="oracle-pro">

:::note

- In the table list, the text colors are changed black → <span class='slow'>orange</span> → <span class='vslow'>Red</span>, which means that the performance of the session is getting slower.

- The <Cmdname sid="process_info" className="uitext" /> can be checked only when the XOS agent has been installed. For more information, see [the following](install-agent#apply-xos-agent).

:::

</Xclude>

<InDoc product="oracle-pro">

:::note

In the table list, the text colors are changed black → <span class='slow'>orange</span> → <span class='vslow'>Red</span>, which means that the performance of the session is getting slower.

:::

</InDoc>
