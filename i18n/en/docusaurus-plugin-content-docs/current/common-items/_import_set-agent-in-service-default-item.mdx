import UI from '@site/src/components/UItext.js';

<ul>
  <li>
    <Cmdname sid="okind_default" className="uitext" />: In the <em>{props.name}</em> file, the options can be commonly applied to the agents classified as <code>whatap.okind</code>, and the applied options can be checked.
  </li>
  <li>
    <Cmdname sid="project_default" className="uitext" />: The options can be commonly applied to all agents belonging to the project, and the applied options can be checked.
  </li>
</ul>
