:::note

Enhance your field-centered user experience by trying out the <Cmdname sid="side_eventSetting" className="uitext" /> <code className="newfunc">New</code> menu.

- Notifications can be sent to desired users by setting **Recipient tags**.

- You can quickly and easily set desired alert notification events through predefined templates.

- The **Simulation** feature allows you to predict the outcome of events.

It can be checked in the ![Icon](/img/ic-sitemap.svg) <Cmdname sid="sitemap" className="uitext" /> &gt; <Cmdname sid="side_event" className="uitext" /> &gt; <Cmdname sid="side_eventSetting" className="uitext" /> <code class="newfunc">New</code> menu path. For more information about the basic event configuration, see [the following](basic-event-setting).

:::
