## Next steps {#nextstep}

<div className="nextstep">

- **Checking the installation**

  If you have created a project, installed an agent, and applied all agent options, see the checklist in [the following](after-install-agent).

- **Installation troubleshooting**

  It provides various problems that may occur when installing the agent and specific instructions for resolving them. For more information, see [the following](troubleshooting).

- **Agent setting**

  <InDoc product="oracle-pro,mssql">

  It provides various features for monitoring by applying some options to the agent configuration file (*whatap.conf*). For more information, see [the following](agent-settings).

  </InDoc>

  <Xclude product="oracle-pro,mssql">

  It provides various features for monitoring by applying some options to the agent configuration file (*whatap.conf*). For more information, see [the following](agent-dbx-settings).

  To additionally monitor the database server resources, set more options in the additional agent (XOS). For more information, see [the following](agent-xos-settings).

  </Xclude>

  <InDoc product="cubrid">

  For more information about the XCUB agent configuration, see [the following](agent-xcub-settings).

  </InDoc>

<InDoc product="postgresql,mysql,oracle,mssql">

- **Cloud Settings**

  To additionally collect and monitor monitoring metrics provided by cloud services on the dashboard of the database project, see [the following](cloudsettings).

</InDoc>

- **Starting the monitoring**

  After configuring all settings, the agent starts collecting metrics data from the database server. First, check whether the monitoring data has been collected in <Cmdname sid="side_instanceList" className="uitext" />. For more information about <Cmdname sid="side_instanceList" className="uitext" />, see [the following](instance-list).

</div>
