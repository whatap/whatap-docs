1.  Set the transaction start point as follows: 

    ```bash
    hook_service_patterns=com.virtual.web.SimulaNonHttp.process
    ```

2.  Restart it. The `process()` method becomes the **end point** of a new transaction.
