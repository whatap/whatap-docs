Home &gt; Select Project &gt; <Cmdname sid="side_dashboard" className="uitext" /> &gt; <Cmdname sid="side_slowQueryLog" className="uitext" />

It reads the slow query logs from the database and display them as a visual chart. The reference time for a slow query is set in a DB parameter, and the SQL in the slow query log is displayed.

:::info

Even for the same SQL, in case of the **SQL Elapse Map** metric in <Cmdname sid="side_multiInstance" className="uitext" />, an error may occur because the time is calculated based on active sessions.

:::
