## Searching event {#search-event}

You can search the event by event name or metric in the event list. Enter a string in the search field and then select ![Search icon](/img/ico-search.svg).

:::note

After searching for events, you can use the export feature to download only the searched list as a JSON file. For more information about the export feature, see [the following](#export-json).

:::
