{/* 이 문서 넘버링이 1 1 3으로 이어진 이유는 xclude page 활용 문제입니다. 번역 시 주의해 주세요. */}

After creating a **Feature Project** (<code class='Features'>Features</code>), the ***{props.name}*** screen appears. Proceed with installation according to the following instructions. 

1. Check the **project access key**. 

   Project access key is the unique ID for activating the WhaTap services. Select <code class='my'>Getting the access key</code>. 

2. Create the **installation script**. 

   <InDoc pages="kafka-introduction">

   Execute the following command to automatically check the user environment and create an installation script on the server where KAFKA has been installed. 

   ```bash
   curl http://repo.whatap.io/telegraf/feature/kafka/install_kafka_monitoring.sh -o install_kafka_monitoring.sh
   ```

   Execute the following command to install the KAFKA agent.

   ```bash
   chmod +x install_kafka_monitoring.sh
   sudo ./install_kafka_monitoring.sh "x604pf485d1kk-z6q14nuc509pk3-x39moealrfodum" "13.124.11.223/13.209.172.35"
   ```

   </InDoc>

   <InDoc pages="aerospike-introduction">

   Execute the following command to automatically check the user environment and create an installation script on the server where Aerospike has been installed. 

   ```bash
   curl http://repo.whatap.io/telegraf/feature/aerospike/install_aerospike_monitoring.sh -o install_aerospike_monitoring.sh
   ```

   </InDoc>

   <InDoc pages="apache-pulsar-introduction">

   Execute the following command to automatically check the user environment and create an installation script on the server where Apache Pulsar has been installed. 

   ```bash
   curl http://repo.whatap.io/telegraf/feature/apachepulsar/install_apachepulsar_monitoring.sh -o install_apachepulsar_monitoring.sh
   ```

   </InDoc>

   <InDoc pages="vcenter-introduction">

   Execute the following command to automatically check the user environment and create an installation script on the server where vCenter has been installed. 

   ```bash
   curl http://repo.whatap.io/telegraf/feature/vcenter/install_vcenter_monitoring.sh -o install_vcenter_monitoring.sh
   ```

   </InDoc>

   <InDoc pages="nginx-introduction">

   Execute the following command to automatically check the user environment and create an installation script on the server where NGINX has been installed.

   ```bash
   curl http://repo.whatap.io/telegraf/feature/nginx/install_nginx_monitoring.sh -o install_nginx_monitoring.sh
   ```

   Execute the following command to install the NGINX agent.

   ```bash
   chmod +x install_nginx_monitoring.sh
   sudo ./install_nginx_monitoring.sh "x42ut20rmr8rn-z1kli3trqviqul-x5cm4quee6cdmj" "15.165.146.117"
   ```

   </InDoc>

<Xclude pages="nginx-introduction">

3. After configuring and restarting the agent, **start** **monitoring**.  

   <InDoc pages="kafka-introduction">

   To configure the Jolokia agent in KAFKA, execute the following command and then restart it.

   ```bash
   #cd {kafka home directory}/bin
   sed -i '/^#!/a export KAFKA_OPTS='\''-javaagent:/usr/whatap/infra/feature/jolokia-agent-jvm-2.0.1-javaagent.jar=port=8778,host=127.0.0.1'\''' kafka-server-start.sh
   ./kafka-server-stop.sh
   ./kafka-server-start.sh
   ```

   </InDoc>

   <InDoc pages="aerospike-introduction">

   Run the following command to install the WhaTap Server Monitoring Agent and Telegraph Sidecar.

   ```bash
   chmod +x install_aerospike_monitoring.sh
   sudo ./install_aerospike_monitoring.sh "x42ru23mvv8e0-z773a0eqslde4u-z32imrtin4l23j" "15.165.146.117"
   ```

   </InDoc>

   <InDoc pages="apache-pulsar-introduction">

   Run the following command to install the WhaTap Server Monitoring Agent and Telegraph Sidecar.

   ```bash
   chmod +x install_apachepulsar_monitoring.sh
   sudo ./install_apachepulsar_monitoring.sh "x42s823cuq7fg-x7ekkc24qctvr1-z51hfam8avhtls" "15.165.146.117"
   ```

   </InDoc>

   <InDoc pages="vcenter-introduction">

   Run the following command to install the WhaTap Server Monitoring Agent and Telegraph Sidecar.

   ```bash
   chmod +x install_vcenter_monitoring.sh
   sudo ./install_vcenter_monitoring.sh "x42u520ie21jb-z76vslonvu6qp8-x7dv18dj7fknuq" "15.165.146.117"
   ```

   </InDoc>

</Xclude>
